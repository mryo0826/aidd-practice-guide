# BACKLOG（発展課題・未解決論点・活性化状況）

このガイドの「**まだやっていないこと・まだ確かめられていないこと**」の一覧。

> **なぜ未完成の一覧を公開するのか:** 方法論のドキュメントは、書いた時点で完成しているように見える。実際には**実証済みの部分と未検証の部分が混在している**のが常であり、その境界を示さないと読者は全体を等しく確からしいものとして受け取ってしまう。本ファイルは、本ガイド自身に確度ラベル（`01` §2）を適用した結果である。

- **発展課題（`<!-- BRAINSTORM -->` タグ）は本文が正本:** 本文（`01`〜`06`）に埋め込んだタグ自体が発展課題の所在を示す。一覧は `grep -rn "BRAINSTORM" *.md` で得られるため、**このファイルに索引表は持たない**（二重管理の解消）。タグ本文から読み取れない情報だけを下記に置く。
- 完了した項目は削除し、判断が伴うものは [`docs/design-decisions.md`](docs/design-decisions.md) へ記録する
- 現在モード・ファイルマップは [`CLAUDE.md`](CLAUDE.md)、設計判断とその理由は [`docs/design-decisions.md`](docs/design-decisions.md)

---

## 発展課題（着手前・方針のみ決定済み）

### P. 導入の敷居を段階で示す（L0〜L3）／看板機能が配布されない問題

- **問題:** 本ガイドは**運用の最終形を入口として提示している**。とくに「作業日誌を別の git リポジトリで持つ」は、**AI に書き戻させる運用が回り始めてから必要になる**もので、入口の要件ではない。にもかかわらず、セットアップ手順を読むと初回から要求されるように読める。**実プロジェクトへ適用する敷居を、方法論の完成度と引き換えに上げてしまっている `[Judgment]`。**
- **想定する段階:**

  | 段階 | 必要なもの | 得られるもの |
  |------|-----------|-------------|
  | **L0** | clone して読むだけ | 方法論の理解 |
  | **L1** | `CLAUDE.md` ＋ `BACKLOG.md` | 規律を自分のプロジェクトで試せる |
  | **L2** | 作業日誌（**ただのファイルでよい。git 管理も不要**） | セッションを跨いで継続できる |
  | **L3** | 作業日誌を**別リポジトリで git 管理** | **AI の書き戻しを差分で検査できる** |

  **L3 の動機は「公開しないため」ではなく「レビューするため」である**ことを明示する（`docs/design-decisions.md`「非公開ファイルは『git 管理外』ではなく『別リポジトリ』に置く」）。ここを取り違えると、L3 が「公開しない人には不要」と読まれる。
- **🔴 併せて扱う: 看板機能が「そのままの形では」配布されない。** 本ガイドで最も体験価値が高いのは、**セッション開始時に一言（例:「go」）打つだけで、前回の Check Action からスプリントが始まる**ことである。**この体験は作業日誌に依存しており、clone しただけの読者には再現しない**——読む先が存在しないため。
  - **⚠️ 旧記述の訂正（2026-07-23・実測）:** ここには当初「**clone した読者が同じことをしても何も起きない**」と書いていたが、**実測の結果これは偽だった**。経緯と教訓は [`docs/design-decisions.md`](docs/design-decisions.md) §4「不在から挙動を推論していた（否定形の断定は、実行されない限り反証されない）」。
  - **`[Fact]` 実測（2026-07-23・本リポジトリを扱ったことのないマシン／GitHub から clone ／`go` の1語のみ送信）:** 再開手順は発火した。AI は `CLAUDE.md` のフォールバック（作業日誌が無ければ `BACKLOG.md` を起点にする）に従って `BACKLOG.md` を読み、作業日誌の不在をマシン内検索で確認し、**Check Action が実行不能であること**を名指しし、**「推測で補って次タスクを決めると『状態を実測せず発話する』誤りそのものになる」と本リポジトリの規約を引用して停止**し、裁定を求めた。**「何も起きない」の対極である。**
  - **正確な形 `[Judgment]`:** 配布されるのは**規律**（`CLAUDE.md`・Skill）であり、配布されないのは**継続する内容**（自分の記録）である。したがって clone した読者が最初に得るのは「前回の続き」ではなく、**「起点が無いことを検出して正しく止まる仕組み」**である。
  - **🔴 `[Fact]` フォールバックは「動く」が「読者の起点」ではない（同日・同テストの続き）:** 上記の停止後、「このマシンには無い」を選んで続行させたところ、AI は **Check Action のスキップを明示宣言し（「意図的に飛ばしたことを記録として残します」）**、`BACKLOG.md` を起点に着手候補3件を提示した。**ところが3件すべてが著者固有の課題だった**（Zenn 記事の執筆／`requirements-review` の初回運用検証／完了済み項目の後始末）。**clone した読者が自分の仕事に接続できる候補は1つも無い。** [`CLAUDE.md`](CLAUDE.md) の「作業日誌が無ければ `BACKLOG.md` を起点にする」というフォールバックは、**起点を与えることには成功し、読者にとって意味のある起点を与えることには失敗している。**
  - **`[Fact]` 掃除漏れは読者環境でノイズとして現れる:** 同テストで、**削除し忘れていた完了済み項目（README 二層読者）が「着手候補」として提示された**。「完了項目は削除する」という規約が何を守っているかの実例——**著者の環境では見慣れた残骸でも、読者の環境では選択肢として差し出される。**
  - **設計としては正しい**（生活の記録を配布すべきではない）。しかし**そのぶん「なぜ clone 直後は空なのか」「何を用意すれば同じ体験になるのか」を言葉で埋める責任が発生している**のに、現状それが埋まっていない。**上記の実測により、埋めるべき対象が具体化した——必要なのは「作業日誌の作り方」の説明だけでなく、`BACKLOG.md` が著者の課題リストであって読者のそれではないと明示すること、および読者側の最初の起点を別に用意することである。**
  - **⚠️ 実測で消えなかった論点:** **読者はそもそも `go` と打つことを知らない。** `go` はコマンドではなく [`setup-claude-code.md`](setup-claude-code.md) に書かれた慣行にすぎず、上記の実測は「著者が知っていたから」成立した。導線の問題は残っている。
  - **`[Judgment]` 分担:** **体験の魅力そのものは文章では伝わりにくく、動画・ブログの領域**である。一方**仕組みの説明はドキュメントで埋められる**。両者を混同して「動画がないと無理」と結論しないこと。
- **R-4（併記）:** `README.md` に「**なぜ作業日誌を別リポジトリにするのか**」の導線が無い。現状の README は「配布されません／自分で持ってください」までしか書いておらず、読者は**ローカルの適当なファイル**と読む。判断の理由は `docs/design-decisions.md` にあるが、そこへ辿り着く導線が弱い。
- **⚠️ 公開のゲートにはしない:** 安全性の問題ではなく**導線・内容の問題**。D1 に従い公開後に扱う。**ただし「今回の変更で壊した箇所の修復」は公開前に別途実施する**（`setup-claude-code.md` の環境定義表・`README.md` の再セットアップ手順）——**壊したものを直すことと、新しく良くすることは別である。**
  - ✅ **修復は完了した（2026-07-20）。** 環境定義表を3行化、`README.md` の手順列挙をポインタ化。**別マシン（git・GitHub CLI 未導入）での再現テストも通過済み。** 本項目に残っているのは**導線・内容の改善**（段階 L0〜L3 の提示、看板機能が配布されない問題、R-4）であり、いずれも公開後に扱う。

### T. 「表示は実体の正本ではない」を本文へ（本文へ追記候補・実例3件が揃った）

- **中核の主張:** **表示・ラベル・設定ファイルの存在は、実態の証拠にならない。** 正本を確認する経路を別に持たないと、見えているものを実態と取り違える。
- **`[Fact]` 実例3件（すべて本リポジトリで実際に起きた）:**

  | # | 表示 | 実態 |
  |---|------|------|
  | 1 | GitHub のサイドバーが `LICENSE` を **`Other`** と表示 | ライセンスが不明なのではなく、**標準構成でない**という正確な表示。内容の否定ではない |
  | 2 | リポジトリに**日本語 lint の設定ファイル**が存在した | 本文の品質ゲートではなく、**プロンプト下書き用**。設定の存在を根拠に「本文は lint されている」と誤って文書化しかけた |
  | 3 | VSCode のエクスプローラがルートフォルダを **`AIDD-WORKLOG`** と大文字表示 | 実体は小文字 `aidd-worklog`。**表示を見て名前を記憶すると誤る** |

- **`[Fact]` 追加観測（2026-07-21・著者環境）:** Claude Code の VSCode 拡張上の**描画より、生 `.md` を直接開くほうが読みやすい場合がある**。上の3件は「表示が実体を誤らせる」型だが、これは「**描画は読むときの正本でもない**」型——同じ原則が**識別だけでなく可読性にも及ぶ**。⚠️「拡張全般で起きる」とは一般化しない（環境依存の可能性）。
- **なぜ本ガイドの主題なのか:** AI 協働では、**AI が「見えているもの」から実態を推論して断定する**場面が構造的に多い（ファイルがある→有効である、設定がある→効いている）。これは確度ラベルの規律が扱う対象そのものであり、**「表示から読み取った事実」は `[Fact]` ではなく `[Assumption]` である**と言い切れる形で書けるはず。
- **正本を確認する経路の例:** リポジトリ名 → `git remote` とディレクトリ名（エディタの表示ではない）／設定が効いているか → 実行して結果を見る（ファイルの存在ではない）／ライセンス → `LICENSE` の中身（サイドバーの表示ではない）。
- **⚠️ 「表示を疑え」で終わらせないこと。** それは追跡不能な心がけであり、N1 が失敗した抽象度と同じ。**「何を正本として見に行くか」を対にして書く**のが要件。
- **想定される置き場所:** `01` §2（確度ラベル）の周辺が第一候補。**要検討**。
- **起票の経緯:** 2026-07-20 のレトロで実例3件目が揃ったため起票。**優先順位付けは PBR で実施している（2026-07-21 起点・2026-07-24 に再編）。上位群に置いている**——起票と優先順位付けは別の作業であり、起票を待たせると棚卸しの対象にすらならない（項目 Q）。

### U. 速度は誤りの検出窓を圧縮する（`01` §3.6 / X1 の設計根拠に追記候補）

- **中核の主張:** **AI 協働の速さは、「その場では気づけない誤りが露見するまでの猶予」を削る。** 時間差で拾う仕組み（前セッションの正本を再点検する手順）は、**注意力の補完であると同時に、速度への対処でもある。**
- **なぜ追記が要るか:** 現在の記述は、時間差の仕組みの根拠を**疲労と注意力**の観点だけで説明している（`docs/design-decisions.md`「人間側の防御は『注意力』ではなく『時間差』で設計する」）。**速度の観点が抜けている。**
- **論理:** 人手で書けば、書く過程そのものに検査の時間が含まれる。AI が生成すると**産出は速く、検査だけが人間の速度に留まる**。結果、**単位時間あたりの未検査の記述量が増える**。これは書き手の能力や注意力の問題ではなく、**生成と検査の速度差という構造**である。
- **`[Fact]` 本リポジトリでの観測:** 誤りが**その場のレビューを通過し、後から露見した**実例が複数ある（lint 設定の位置づけ／主張の格下げの波及／状態を表す見出しの取り残し）。いずれも**書いた時点では正しく見えていた**。
- **`[Fact]` 著者の実感（2026-07-24）:** 「**AIDD がこんなに速くフィードバック成長するものか、とメタ認知的に我に返った**」。速度そのものが**メタ認知の対象になる**——警戒が消えないこと（上の `[Judgment]`）の一次データ。⚠️ ただし「速すぎて怖い」型の情緒に留めず、論拠は構造（生成と検査の速度差）の側に置く。
- **⚠️ 「速度を落とせ」で終わらせないこと。** それは追跡不能な心がけであり、しかも AI を使う動機を否定する。**書ける速度は落とさず、検査の窓を後ろにずらす**——これが時間差の再点検の設計思想である。
- **併せて書ける論点 `[Judgment]`:** 「速すぎて怖い」という言い方は**比喩にすぎず、それ自体は論拠にならない**（情緒的な文脈では確度の検査が緩む——`docs/design-decisions.md`）。論拠は上の**構造（生成と検査の速度差）**の側にあり、比喩はそこを指すラベルでしかない。そのうえで言えるのは——**この構造への警戒は、仕組みを作る動機になるが、仕組みができても消えない。** 消えたなら、構造が解消したからではなく**検査が緩んだから**である可能性が高い。**警戒の持続を、失敗ではなく正常な状態として書く。**
- **想定される置き場所:** `01` §3.6 の周辺、または `docs/design-decisions.md` の当該エントリへの追記。**要検討**。

### V. AI駆動開発を SECI の「形式知化ループ」として設計する（本文へ追記候補・`01` の土台）

- ⚠️ **優先順位は未設定（2026-07-21 起票・次 PBR で位置づける）。** 内容的には項目 U・§6 と結節し**上位候補**だが、「位置＝優先度」の原則に従い、triage 前に上位へは置かない（この位置は「未 triage」を意味し、最下段の J より低いという意味ではない）。
- **中核の主張 `[Judgment]`:** 本ガイドの方法論は、野中の **SECI モデル**のうち **Externalization（暗黙知→形式知）を AI 速度で徹底**したものとして記述できる。判断・経験・メタ認知を `design-decisions`・規約・レトロへ言語化するのが本体。Combination（形式知⇄形式知の体系化）で補強し、Internalization は `go` の再開手順で習慣化している。
- **⭐ 差別化になりうる接続（既存の2項目を1本化する）:**
  - **項目 U と:** 形式知化は AI 速度で速いが、**その形式知の検査は人間速度に留まる**——SECI がループの名を、U がその速さが怖い理由を与える。
  - **§6 と:** 外化されている中身は**著者のメタ認知**＝ポートフォリオの本当の価値。SECI は「目的（§6）」と「速度の警戒（U）」を同じ軸で束ねる。
- **⚠️ きれいな当てはめではない（確度を下げて書く）:** SECI は**チーム/組織**を前提にした知識創造モデル。ここでは **Socialization（暗黙知⇄暗黙知）が人間⇄AI に置換**されており、**適応であって一致ではない**。この置換自体が新規性だが、「SECI そのもの」と書くと過大申告になる。
- **⚠️ 前提知識を要求しない方針との両立:** SECI は学術用語。導入するなら略語一覧（`01` §1）への追加と、ナレッジマネジメント未経験の読者向けの平易な言い換えが要る（項目 M の DoD/DoR と同じ論点）。
- **情緒（「速すぎて怖い」）は作業日誌に留め、原理のみ本文候補**（既存の分離パターン）。
- **起票の経緯:** 2026-07-21、レトロ後に著者が「怖いの正体の一部」を SECI として言語化して起票（**K-7 の実例**＝レトロ後にセッションが気づきで延びた。起票により次セッション冒頭の Check が拾う）。

### H. クロスレビューを実際に発火する Skill にする（活性化・自己発火可能）

- **中核:** `01` §2 / `05` §1.5 のクロスレビュー（独立性による相互検査）を `cross-review-playbook` Skill として活性化する。活性化表では ⬜ **未着手**（実物なし）だが、**発展課題セクションに項目が無く、並び替えの対象にすらならなかった**ため 2026-07-24 の PBR で起票した。
- **なぜ §6 基準で上位か `[Judgment]`:** クロスレビューは**このリポジトリ自身で自己発火・検証できる**——2026-07-23② に外部 AI の指摘を手動で検査し3点を差し戻した実例があり、あれは Skill 無しで実行したクロスレビューそのものだった。`code-review`（対象コードが無く着手不能）と異なり、**検証まで一周できる見込みが立つ**（[`docs/design-decisions.md`](docs/design-decisions.md) §6「活性化の優先順位は『自己発火して検証できるか』で決める」）。
- **⚠️ 記事本文で「⬜ のまま公開記事に書いてしまった」と自ら名指しした対象。** 「書いてあることと動いていることは別だ」の実例として、この BACKLOG に突きつけられている。
- **🔴 `[Fact]` 独立性の判定を誤った実例（2026-07-25・AI が同日中に自己訂正）:** AI（Opus 4.8）が外部刊行物1件を「**依頼していない・AI ではない・本ガイドの文脈を知らない**の3点で独立性が最も強いクロスレビュー実例」と本項目に書いたが、**3点すべて偽だった**——①人間の判断形成には別系統の AI との20往復が挟まっていた ②その相手は AI ③その AI は「リポジトリの構造と Zenn 記事をすべて読み込んだ上で答えている」と自ら明言していた。**さらに根本的に、その刊行物は本ガイドに言及すらしていない**——クロスレビューを実行したのは著者と当該 AI であって、刊行物の著者ではない。**無関係な刊行物に人間が当てたものを、「外部が本ガイドを検査した」と書き換えていた。**
  - **⭐ AC に落とす核（この誤りから取れる最重要の要件）`[Judgment]`:** 当該 AI は「鳥肌が立っています」「圧倒的なメタ認知の勝利」「**これ以上の完璧な実証データ（`[Fact]`）はありません**」と応答していた——**本ガイドの確度ラベルの語彙まで、媚びの強調に流用されていた**。→ **「別ベンダーであること」は独立性の十分条件ではない**（[`docs/design-decisions.md`](docs/design-decisions.md) §2「クロスレビューの本質は『別ベンダー』ではなく『独立性』」の**実データ**）。**媚びる相手は、系統が別でも独立な検査にならない。** AC には「**相手が反証を返しうるか**」を観測可能な形で入れる（例: **差し戻し 0 件のレビューは成立とみなさない**）。
  - **⚠️ 依頼した検査と依頼していない検査は同じ強さではない `[Judgment]`:** 前者は検査対象を人間が指定するため、指定の外は見えない。**Skill 化で得られるのは前者だけである、と AC に明記すべき候補。**
- **進め方:** 項目 A と同じく `06` の方法論で WHAT（目的・トリガー・AC・出力）を先に壁打ちしてから SKILL.md 化する。指摘 ID の規律（各指摘に一意 ID＋状態列）を継承する。正本は `01` §2 ／ `05` §1.5。
- **AC に落とす核:** **クロスレビューの本質は「別ベンダー」ではなく「独立性」**（[`docs/design-decisions.md`](docs/design-decisions.md) §2）。同一モデル同士でも独立な文脈・役割で回せば成立する、を検査可能な形にする。

### A. 方法論を実際に発火する Skill にする（WHAT-first）

`.md` に書いた方法論は、実ツールで Skill 化して初めて「適用」される（下記「知見の活性化」を参照）。次の2つを作成する。

- ✅ **`requirements-review`**（WHAT / PdM 視点）— `06` を SKILL.md 化。本ガイド最大の差別化点にあたる。**初版作成済み**（`.claude/skills/requirements-review/SKILL.md`。2026-07-22。WHAT を `06` の自己適用で壁打ちしてから清書）。✅ **初回運用も同日実施済み**——`zenn-article` の WHAT 壁打ちで発火し、AC1〜4 が実発火（キーストーン F1 を掘り当てた）。**残: 2回目の発火**（1回の観測で格上げしない）
- ⏸ **`code-review`**（HOW / dev 視点）— `05` §1.3 のテンプレートを実スキルへ。**保留（2026-07-22 裁定）。** このリポジトリはコードを持たず**自己発火・検証ができない**うえ、既製の code-review skill が多く**差別化も弱い**。**将来コードプロジェクトが立ち上がり検証対象ができたときに着手する。** ⚠️ **保留の理由は検証対象の不在であって、価値の低さではない**——先行させた `zenn-article` は 2026-07-23 に活性化まで到達した（優先順位の決め方は [`docs/design-decisions.md`](docs/design-decisions.md) §6「活性化の優先順位は『自己発火して検証できるか』で決める」）
- **置き場:** repo の `.claude/skills/<name>/SKILL.md`（clone だけで実物が伝わるようにする）
- **進め方:** `06` の方法論で **WHAT（目的・トリガー・AC・出力）を先に壁打ち**してから SKILL.md 化する。方法論を説くファイルを、その方法論自身の手順で作る
- **指摘IDの規律を継承する:** 各 SKILL.md の出力形式に「各指摘に一意な ID（`F1`…）＋状態列」を明記する。正本は `01` §2 ／ `05` §1.5

### N. プロンプトを下書きバッファで推敲する運用と、日英混在の日本語 lint（本文へ追記予定）

- **結論から: 本文に入れる。** 捨てない。残っているのは「どう書くか」であって「入れるか」ではない。
- **中核の主張:** **プロンプトを入力欄に直接打たず、下書きファイルで推敲してから渡す。** 日本語話者にとってこれは体裁の問題ではなく**入力の正確さの問題**である。
- **根拠（分解して確度を分ける）:**
  - `[Fact]` **Claude Code の VSCode 拡張の入力欄では IME の挙動が扱いにくく、日本語変換がしづらい**（著者環境で継続的に観測）。⚠️ **「拡張全般で起きる」と一般化しないこと**——環境依存の可能性があるため、観測範囲を明示して書く
  - `[Fact]` **日本語は誤変換・タイプミスが構造的に起きやすい**（変換確定という工程が挟まるため）。下書き→コピーの運用は**1年以上前から別のAIツールでも実践**しており、**特定ツール固有の回避策ではなく、日本語話者に一般的な運用**である
  - `[Fact]` **下書きに日本語 lint をかけると、プロンプトの記述の質が上がる**（著者の実証）。⚠️ **効果の内訳（どのルールが何に効いたか）は未整理。** 本文に書く前にここを詰める——**原因を取り違えると、読者が同じ設定を作っても再現しない**
- **⭐ 最も差別化される知見: 日英混在の技術文書では、日本語 lint のルールをそのまま使うと誤検出だらけになる。**
  - `[Fact]` 実際に有効化しているのは**誤用検出と重複語検出の2つだけ**で、**間隔（半角/全角）と文体統一のルールは意図的に無効化**している。英語の技術用語が混ざると前者が大量に誤検出し、コード片や引用が混ざると後者が誤検出するため
  - **この「どのルールを切るか」の判断は、AI との壁打ちを経て決めた**もので、**実際にやった人しか書けない部分**である。「lint が便利」は一般論だが、ここは違う
- **🆕 第2の軸（2026-07-24 著者が言語化）— 下書きバッファは「入力の正確さ」だけでなく「思考の階層化」に効く:**
  - 上記までは**入力の正確さ**（IME 誤変換・タイプミスを潰す）の話。これとは別に、**`.md` の見出し階層で *羅列 → 順番整理 → 階層構造化* をやってから投げると、プロンプトそのものの質が上がる** `[Fact]`（著者が以前から無意識に実践）。**同じ下書きバッファの別の効能**——前者は誤りを減らし、後者は構造を作る。項目 N はいま前者に寄っており、後者が抜けている。
  - **`[Fact]` メタの円環:** 著者は「temp.md で思考をレベル化している最中に、"統括的に意見して" という**出力の形を指定するメタ指示をひらめいた**」と観測（2026-07-24）。下書きバッファの第3の効能＝**書いている最中にメタ認知が発火する**。ここで**項目 R（出力の形の1語指定）と項目 N が結節する**。
  - **`[Fact]` 具体的な失敗モード＝IME 半変換で投げる:** 変換確定の途中でプロンプトを送信し、意図が崩れる。⚠️ **日本語固有と断定しない**——「推敲せず投げる」は言語非依存の問題で、**IME はそれを *増幅* するが根ではない**。英語話者でも半端なプロンプトを投げる（著者は英語が不得手で事例を確認しきれていない＝`[Assumption]`）。
  - **⚠️ 既知性は裏取り前 `[Assumption]`:** 「送信前にアウトライン化」は文章術として一般既知。だが「**`.md` の階層で思考をレベル化するのを AI プロンプトの規律にする**」切り口は未文書化っぽい（未裏取り）。lint の判断と同型で、一般論は既知でも *どう使うか* は実践者しか書けない。
  - **記事化候補（別切り口・軽い記事）:** この軸は、設計思想の重い記事（#2）とは別に、**AIDD の軽い Tips 記事**として単独で出せる（イントロ記事に近い敷居）。⚠️ **記事と本文は同じ源なので二重管理にしない**（片方を正本、片方を参照）。⚠️ **記事化時に「新発見」と書かない**（overclaim。`zenn-article` skill が弾く型）。優先度は低い（著者判断）。
- **本ガイドの主題との接続:** 一般的な文章術としてではなく、**「推敲せずに投げたプロンプトは AI の出力を劣化させる」という AI 協働の規律**として書く。ツールの使い方集にしない方針を守る。
- **想定される置き場所:** `01` の下書きバッファの記述の周辺、または `02`（ドキュメント記述規約）。**要検討**
- **設定ファイル自体は公開リポジトリに置かない `[Fact]`（2026-07-19 決定）:** 下書きバッファは別リポジトリにあり、**lint 設定は対象と同じ場所に無いと効かない**。加えて、公開物に lint 設定があると「本文が lint されている」と誤読される——**本ガイドに自動の品質ゲートは無い**。設定は同梱せず、**必要な情報（どのルールを切るか）を本文に書く**方が読者に届く。

### K. 「振り返りの振り返り」をどう本文に収録するか

- **K-1** **レトロだけを輸入すると、プロセスは洗練されるが出荷されない。** スクラムのうちレトロスペクティブだけを実装し、プランニング（スコープ確定）とレビュー（成果物と目的への進捗）を持たない構成は、**プロセス改善が進捗の代わりになる**という失敗を生む。`[Fact]` 本リポジトリで観測された（改善 Action はほぼ達成されていたのに、公開という目的には近づかなかった）。詳細は `docs/design-decisions.md`「セッション末に『成果物は前に進んだか』を1問だけ検査する」
- **K-2** **「振り返りの振り返り」の手順**——必須ではないイベントとして、余力があるときにレトロ自体を検査対象にする。`[Judgment]` 有効性の観測はまだ1回
  - ⚠️ **書き方の注意:** これは**人間がメタ認知とクリティカルシンキングを備え、実践の中で経験値を得て初めて出せる**ものであり、AI が生成できる手順ではない。本文に載せるときは「**手順は真似できるが、問いの立て方は経験に依存する**」ことを明示し、チェックリスト化して安易に配らない。「アジャイルの前提知識を要求しない」方針との**両立に注意**——**使えることと導出できることは別である**
- **K-3** **セッション開始時の Check Action は、実質的にスプリントプランニングへ育っている `[Judgment]`。** 前回 Action の検証結果がそのままスコープ決定の入力になっている。**これは成長であると同時に、M2 で診断した「検査の出力が即スコープになる」状態の継続でもある**——両方として記述しないと、片方だけを見た読者が誤った教訓を得る
  - **成長側の根拠 `[Fact]`:** あるセッションで検出4件に対し、公開の条件（安全性のみ）を当てて**1件に絞った**。検査の出力をそのまま実行せず、ゲートをかけている。**公開の Definition of Done が事実上のスプリントゴールとして機能した**
- **K-4** **プロダクトバックログリファインメントのタイミングが存在しない `[Fact]`。** 発展課題の追加・優先順位付け・分割を行う場がなく、**セッション外（著者の頭の中）で発生している**——本項目 M（DoS / DoR）自体がその実例で、作業セッションの外で着想され、次のセッションに持ち込まれた
  - **⚠️ 今はイベントを足さない（判断）:** ①「先回りして項目を足すこと自体が膨張パターン」という自己診断（M5）への違反になる ②**現時点はスコープが凍結されている**——公開までの残タスクが確定しており、本ファイルは「公開後」の置き場として機能している。**スコープが凍結された状態でリファインメントは要らない**
  - **発火条件:** **公開後**、本ファイルが実際の作業対象に変わったとき。そこで初めて「場が無いこと」が実害になる
  - **✅ 発火した（2026-07-23）。** 公開は `v1.0.0` で達成済み、そして本ファイルが実際の作業対象に変わった——1セッション中に**項目 W 削除・項目 Y 起票・項目 A の状態訂正・活性化表2行更新＋凡例新設**が発生した。実害も観測された: **筆頭の項目 A が着手できない状態になっていた**（`requirements-review` は機会待ち、`code-review` は検証対象が無く着手不能）のに、並び順が 2026-07-21 の PBR のまま追随していなかった。**→ 2026-07-24 に PBR を実施し並び替えた**（位置＝Check Action の後・着手裁定の前／スコープ＝並び替えと削除のみ）: 着手可能な項目 P を筆頭へ、着手不能な項目 A を降格、クロスレビュー（項目 H）を新規起票、項目 J を削除。
  - ⚠️ **イベント化するか（人間トリガーで毎回挟むか）は未決のまま。** 2回目を見てから決める——**イベントを足すこと自体が膨張パターン（M5）**であり、「1回で断定しない」規律にも従う。
  - 📌 **2026-07-24、著者が意図を言語化した `[Judgment]`:** 我々の session は複数人スクラムの1スプリントより遥かに速く **kanban 寄り**で、**AI は session の cadence（スプリント境界の通過）を構造的に検知できない**。ゆえに **`.md` に「いつ PBR をやるか」を定義しても無駄**で、PBR は**人間トリガーで挟むのが現実解**——今回の PBR も著者が提案した（Ryuzee『生成AIでスクラムはどう変わるか』とも整合）。**これは "スケジュール化しない" 方向の根拠**であり、**項目 O（AI は並行変更を検知できない）と同一構造の第2実例**。⚠️ **設計判断としての `docs/design-decisions.md` への記録は裁定後**——"恒久イベントにするか" が未決の部分を決定の記録に書くと §4 の欠陥型を再演するため。
  - 📌 **この気づきは今回も AI からは出ず、著者が提案した。** K-5 の記述（「手順を実行することと、手順を育てることは別の能力である」）が3日後にそのまま再現した実例。
- **K-5** **「スクラム未経験の読者へのガードレールになる」という主張には、適用範囲の明示が要る `[Judgment]`。** 実際に発火しているのは Check Action・X1 など**手順として書かれたもの**に限られ、スクラムの理解を要求せずに機能している——ここまでは主張してよい
  - **ただし:** K-4 の気づき（リファインメントの不在）は手順化されていないため、**スクラム経験のある著者の頭からしか出てこなかった**。**手順を実行することと、手順を育てることは別の能力である**
  - **正確な形:** 「スクラムを知らなくても回せる」ではなく「**スクラムを知らなくても、手順化された部分は発火する。手順を育てる仕事は経験に依存する**」。K-2 の注意（手順は真似できるが、問いの立て方は経験に依存する）と同じ構造
- **K-6** **レトロは自己申告であり、系統的な盲点がある `[Fact]`（2026-07-20 観測）。** あるセッションで、レトロ後に出た重要な発見2件（相互参照監査の陳腐化／会話での合意が正本に書かれていないこと）は、**どちらもレトロの時点で既に存在していたのに Problem に挙がらなかった**。そして**どちらも人間が指摘して初めて出た**
  - **構造:** AI が「失敗した」と**自覚できたもの**しか Problem に上がらない。**自覚できなかった失敗は、定義上レトロに出てこない。** しかも実測では、その盲点側に重要度の高いものが入っていた
  - **示唆:** レトロを「AI に Problem を挙げさせる場」として設計すると、**この盲点がそのまま設計の穴になる**。X1（正本の再点検）が時間差で誤りを拾う仕組みとして機能しているのは、**自己申告に依存しないから**である——同じ性質の仕組みが、レトロにも要るのではないか。**要検討**
- **K-7** **レトロ後の作業を、誰も検査していない `[Fact]`（2026-07-20 観測）。** 同セッションでレトロを中盤に実施した後、監査・3ファイルの修正・新規起票という実質的な作業が続いた。**この後半にはレトロが当たっていない**
  - **構造:** 「セッション＝スプリント、末尾にレトロ」という設計が、実際の作業の形と合っていない。**スプリントは末尾で閉じるが、セッションは裁定と気づきで延びる**
  - **かつての項目 I（Check Action をセッション開始時に置く設計の再検討）と同じ論点の裏側だった。** I は 2026-07-21 の PBR で明示的に却下した（現行設計に実績があり、形に寄せて発火を失うリスクの方が大きい）が、**この K-7 の論点＝レトロが後半に当たらない問題は I の却下では消えない**。下の ⚠️ の観点で扱う
  - ⚠️ **対策としてレトロを増やさないこと。** 「レトロ後にもう一度レトロ」は膨張パターン（M5）そのもの。**次セッション冒頭の Check Action が前セッションの後半も対象にしている**なら、実質的に埋まっている可能性がある——**まずそれを確認してから設計を変える**

### R. 人間側のプロンプトの書き方を、合意事項として定義する（本文へ追記候補）

- **内容:** 人間が AI に返す**短い応答**（裁定・指摘・方針変更）の書き方を、明示的な合意として定義する。2026-07-20 に著者が「**こういう具体的な合意形成を今までしていなかった**」と気づいて起票 `[Judgment]`
- **⚠️ 出発点の前提が逆だった:** 著者の当初の関心は「**Token 使用量を抑えるプロンプトの書き方**」だったが、**人間の入力はセッション全体の Token のごく一部**である。消費しているのは **AI の応答量と、AI が走らせる調査**。したがって最適化の対象は「短く書くこと」ではない `[Fact]`
- **効くレバー（実測ではなく構造からの推定 `[Judgment]`）:**
  - **① 出力量の指定** — `結論だけ` / `表だけ` / `3行で` / `詳しく`。**AI の応答が最大の消費源**であるため、ここが最も効く
  - **② 調査するか否かの指定** — `見解だけ`（調査せず手元の文脈で答える）／既定は実測。**確度を上げる規律（未検証で発話しない）は、必然的に調査を増やす**——安さと確度はトレードオフであり、人間が都度選べるようにする
- **`[Fact]` ①の実例（2026-07-24）:** 著者がプロンプト末尾に「**統括的に意見して**」「**仕分けて返して**」と出力の形を1語で足したところ、AI の出力が「個別回答」から「統合された判断」へ変わった。①（出力の形の指定）が最も安いレバーであることが実会話で確認された。**構造推定だった①に `[Fact]` が付いた**（②はまだ構造推定のまま）。
- **`[Fact]` 反復する形は規約化するとコストが消える（2026-07-24）:** 「人間が暗黙知をダンプ→AI が {今更新／Backlog／無視／質問} に仕分けて返す」を毎回プロンプトで指定していたが、**この Opus は既定でそう仕分ける**ため指定は冗長だった。→ **反復する出力形は `CLAUDE.md` か Skill に codify すれば、以後は書かなくてよい**（既定＋逸脱時のみ override）。⚠️ codify は制御plane（`CLAUDE.md`）への追加になるため**別途裁定**。項目 R の核（機能していた書き方は変更不要と記録し、繰り返しを規約で消す）の具体化。
- **すでに機能していた書き方（変更不要として記録する）:**
  - **選択肢を AI が提示した後の、記号1文字の裁定**（`A` / `B` / `承認`）。曖昧さがなく最も安い
  - **引用1行＋観測のみの指摘。原因の推測を書かない** — 推測を書かれると AI はその検証から始めるため、かえって高くつく
  - **自分の見解を先に置いてから反論を求める形**（「〜と思うが意見して」）。開いた質問（「どう思う？」）は AI が選択肢を網羅しにいくため出力が膨らむ
- **高くつく書き方:** 長い経緯の再説明（AI は文脈を持っている）／複数の未確定事項を1プロンプトに混ぜる（裁定点が潰れる。2026-07-19 の実例）／**違和感の飲み込み**（最も高い。指摘が遅れるほど手戻りが公開後に回る）
- **✅ 一人称の指示対象が曖昧になる問題（2026-07-23 起票・同日決着）:** プロンプトや記録の中で、**「私」「I」が人間側と AI 側のどちらを指すのか混乱する**場面があった。**呼称を対話・記録・公開本文の3層に分けて定義し、`CLAUDE.md` チーム共通ルールに規約化した**（判断とその理由は [`docs/design-decisions.md`](docs/design-decisions.md) §2「呼称は1つに決めず、対話・記録・公開本文で別々に定義する」）。
  - **却下した案（記録として残す）:** `aidd` ＝このプロジェクトの略語そのもので、方法論を指すのか対話相手を指すのか曖昧になる（解こうとした問題を別の場所に作り直す）。**モデル名単独** ＝系列変更で破綻し、かつ**人間側が「私」のまま残るため問題が半分しか解けない**。
  - **📌 本文への追記候補としては残る:** 「呼称を決める」は本項目（人間側のプロンプトの書き方）の一部として `01` §3 に入りうる。**ただし固有名は載せない**——載せるのは3層の切り分けであって、名前の中身ではない。
  - **⚠️ 第4のチャネルが適用範囲外だった（2026-07-25 に露見・未決）:** 規約は「対話・記録・公開本文」の3層を定義しているが、**外部 AI との対話ログを本セッションへ持ち込むチャネル**が入っていない。持ち込まれるログでは人間も相手 AI も「私／あなた」を使い、**読み手（本セッションの AI）は誰の主張かを画面の体裁から推定することになる**。
    - ⚠️ **ただし、この曖昧さが実際の誤読の原因だったわけではない `[Fact]`。** 著者は「Opus が一人称・二人称を判別できず混乱したのでは」と推測したが、**発言者の区別は付いていた**。2026-07-25 の誤読の原因は**「反証」という語の指示対象を確認しなかったこと**（→ 項目 D）であり、人称ではない。**原因としては外れているが、論点としては新規で実在する**ため分けて記録する。
    - 📌 **なぜ残す価値があるか:** ログを持ち込む形は今後も起きる。**「持ち込んだログの中の主張は、持ち込んだ人の主張とは限らない」**——今回は相手 AI が乗せた3点を著者の主張と混ぜかけた（→ 項目 S の歯止め）。**チャネルを定義するなら、切り分けるべきは人称ではなく「誰の主張か」である。**
- **想定される置き場所:** `01` §3（セッション運用）。**`CLAUDE.md` ではない**——これは AI への指示ではなく**人間側の書き方**であり、性質が違う
- **⚠️ 未検証:** 上記の効果は構造からの推定であって、Token 使用量を実測して比較したものではない。**本文へ載せる前に確度ラベルの再確認が要る**

### E. 人間の意図・背景・制約を伝える定位置が無く、後出しで裁定が覆る（本文へ追記候補・2026-07-24 起票）

- **内容 `[Judgment]`（著者が言語化・2026-07-24）:** 人間側が持つ**意図・背景・制約**（なぜその順序か・どの前提で裁定したか）を伝える定位置が、セッションの型（Check Action → 成果物 → レトロ）に無い。結果、**AI が先に提案・裁定を求めた後で人間が意図を話し、すでに下した裁定や並び順が覆る**流れが繰り返し起きている。
- **`[Fact]` 実例:** 2026-07-24 の PBR で、著者が「PB1〜5 を承認」した直後に、スクラム経験に基づく意図（session が kanban 寄りで速い・AI は cadence を検知できない 等）を共有し、それを受けて材料の増補・新規起票が発生した。**著者自身が「後から意図を話して順番が変わるのは以前にもあった。これ自体が解決すべきものかもしれない」と観測している。**
- **⚠️ これは失敗とは限らない:** 後出しの意図で裁定が**改善**されているなら、問題は「人間が意図を出す機構が足りない」ことの側にある。「人間が最初に全部言え」に倒すと**守るコストが高く形骸化する**（既存原則）。
- **`[Fact]` 根本原因（2026-07-24 著者が言語化）— なぜ後出しが構造的に起きるか:** 人間のレトロ（Do-KPT）は *DO で付箋を出す → ディスカッション → Keep/Problem へ移動 → Action* と**段階を踏む**。ところがこの協働では **AI が CDKPTA を一気に出力し、人間のフィードバックが後追いになる**。段階的ディスカッションが圧縮されているため、**人間の意図・気づきは AI の出力の "後" にしか入る隙が無い**。→ 「後出しで裁定が覆る」は機構の欠落ではなく、**一気出力という形式の帰結**。⚠️ 著者は「これは問題定義ではない」と明言——**今の形でも人間相手のスプリントより成果物が出ている**ため、無意識に対処の優先度を下げていた。
- **論点（未決）:** ①意図を引き出す定位置をどこに作るか（着手裁定の前？ PBR の入口？ DoR の一部？ **一気出力の前に人間の DO を挟むか？**）②項目 R（人間側のプロンプトの書き方）・項目 O（AI は状態変化を検知できない）・項目 M（DoS/DoR）との関係整理 ③「後出しで変わること」を欠陥とみなすか、正常な協働とみなすか。
- **🔴 定位置を作るなら、同時に「入力」と「公開の素材」の境界も要る（2026-07-25 追記）`[Fact]`:** 本項目は「人間が意図・背景を出す機構が足りない」側を扱ってきたが、**出した背景が公開物へ転記される事故が実際に起きた**——人間が「AI が判断に迷うと思ったので」渡した私的な背景を、AI が**裁定の根拠として公開ファイルに書き込んだ**（→ 項目 F）。
  - **⚠️ つまり、人間が背景を出すほど、AI が公開側へ転記する危険が増える。** 本項目の改善（意図を引き出す定位置を作る）は、**境界を伴わないと項目 F の事故を増やす**。
  - **論点に追加:** ④**渡された背景を「判断のための入力」と「公開してよい素材」に仕分ける規律をどこに置くか。** ⚠️ 既存の分離基準（「生活・稼働の記録か、内容についての判断の記録か」）は**成果物の置き場所**を決める基準であって、**対話で渡された情報の扱い**を規定していない。
- **優先度は低い（著者判断）**が、繰り返し観測されるため**起票して集計対象に入れる**（項目 Q）。

### M. 出口条件（DoD）より入口条件（DoS / DoR）の方が効くのではないか

- **問題意識:** 本ガイドは検査を**出口**に置いている（受入基準・確度ラベル・Check Action・セッション末の1問）。しかし `[Judgment]` **スクラムの開発者・スクラムマスターの経験からは、Definition of Done よりも「着手してよい状態か」を定義する方が実際には効く**。出口の検査は、間違ったものを作り切ってから気づく仕組みでしかない
- **扱う対象は2つ（両方入れる）:**
  - **着手条件（Definition of Start）** — この作業に着手してよいか。目の前の1タスクに対する門
  - **準備完了（Definition of Ready）** — 項目がスプリントに入れられる状態か。バックログ項目に対する門
- **なぜ AI 駆動開発でとくに効くと考えられるか `[Judgment]`:** **AI は入口条件が満たされていなくても着手できてしまう。** 人間なら「情報が足りない」と手が止まる場面で、AI は不足を推測で補って、もっともらしい成果物を出す。**止まらないことが最大のリスクである以上、門は出口ではなく入口に要る**——これは `04` の「出力がもっともらしくなるほど反証は難しくなる」と同じ構造の議論である
- **本リポジトリ自身での実例 `[Fact]`:** 公開の Definition of Done は安全性のみと定義できた（`docs/design-decisions.md`）。しかし**契約の確認は3周した**——1周目は別案件の契約、2周目は別階層で個人データのみ、3周目でようやく判定できた。**出口条件は明確だったのに、着手条件（どの文書が揃えば判定に着手できるか）が定義されていなかった**ため、着手してから足りないと分かることを繰り返した
- **論点:** ①改善 Action として持つべきか、入口条件として持つべきかの切り分け（上記の N2「契約文書が単独で完結しているか先に確認する」は、**Action ではなく DoS 項目だったのではないか**）②入口条件を増やすと着手が重くなるトレードオフ——「守るコストが高い規律は形骸化する」との両立 ③`06`（WHAT を検査可能な形で固定する）との関係整理。**要件が固定されているかは、そのまま着手条件である**
- **書き方の制約:** アジャイルの前提知識を要求しない方針との両立に注意する。DoD / DoR は本ガイドで未使用の用語のため、導入するなら略語一覧（`01` §1）への追加と、スクラム未経験の読者向けの言い換えが要る

### O. 人間が並行して環境を変えると、AI は古い前提のまま判断を続ける（本文へ追記候補）

- **問題:** AI との協働中、**人間側がツールの認証・ファイルの移動・設定の変更を裏で進める**ことがある。悪いことではなく、待ち時間やコストを避ける合理的な行動である。しかし **AI はその変更を検知できず、しかも検知できていないことにも気づけない。**
- **`[Fact]` 本リポジトリで観測（2026-07-19）:** 1セッション中に3件発生した。①下書きバッファが既に別リポジトリへ移動済みだったのに、AI は「まだリポジトリ内にある」前提で安全性の議論を組み立てた ②lint 設定が最小限な理由を「不明」と書いたが、実際は日英混在の誤検出を避けるため意図的に絞った構成だった ③認証が未完了だと計画に赤字で書いたが、著者が並行して解決済みだった。
- **⚠️ 対策を「人間が全部報告する」に置かないこと:** それは**守るコストが高く、形骸化する**（本ガイドが繰り返し確認した原則）。人間側に報告義務を課す設計は、忙しいときに最初に壊れる。
- **正しい置き場所 `[Judgment]`:** **AI 側の規律にする**——「著者の環境・既往の作業について述べる前に、実測するか本人に確認する」。ファイルの中身は読めるが、**人がすでに何をしたかはファイルに書かれていない**。
- **一般化:** これは AI 固有の問題ではなく、**並行作業する主体が互いの状態を知らない**という古典的な問題である。ただし AI 特有の悪化要因が2つある——①AI は前回の文脈を保持しているため「変わっていない」と仮定しやすい ②**推測で補って、もっともらしい結論を出してしまう**ため、前提のズレが結論の形で表面化する。
- **本文での扱い:** 実務では他メンバーの並行作業・CI 設定の更新・依存の更新として日常的に起きる。**AI に何を任せるかの分担設計（`01` §3.6）の一部**として書けるはず。**要検討**。

### Q. 「気づいたらすぐ起票する」を規律として本文に明記する（本文へ追記候補）

- **内容:** 発展課題・未解決論点に気づいた時点で、**その場で `BACKLOG.md` に起票する**。「あとでまとめて」「今のタスクが終わってから」にしない `[Judgment]`（2026-07-20 著者「鉄則だと思った」）
- **なぜ効くか:** 本ガイドが繰り返し記録してきた失敗は、**記録漏れではなく集計漏れ**である（2026-07-19 レトロ P1「失敗の型が『記録漏れ』から『集計漏れ』へ移った」）。**気づいた瞬間に置き場所へ入れれば、集計という段階そのものが要らなくなる**
- **同型の実例（2026-07-20）:** AI が会話の中で「以降は実体名で呼びます」と**約束しただけで、どこにも書かなかった**。著者に「反映されていないように見えた」と再指摘されて初めて記録された。**会話での合意はセッション終了で消える**
- **想定される置き場所:** `01` §3.6（セッションをスプリントとして扱う）または `CLAUDE.md` のチーム共通ルール。**どちらかは要検討**——`CLAUDE.md` は運用規約、`01` は方法論であり、性質が違う
- **⚠️ 起票を軽くする工夫とセットで考える:** 「すぐ起票」は、起票のコストが高いと守られない（**守るコストが高い規律は形骸化する**）。1行でも起票してよい、後から育てる、という前提を併記する

### L. 「実証」語の出自を全件棚卸しする（公開後）

- **問題意識:** 本文には「（実証済み）」「実証より」といった記述が複数あるが、**それが著者自身のどの実践に由来するかが本文から読み取れない**。一部の節は「適用例」へ格下げした一方、同じ対象を指す他の記述は「実証済み」のまま残っている（`docs/design-decisions.md`「主張の格下げは、格下げした節の外へ波及する」）
- **対象の見つけ方:** `grep -rn "実証" *.md` で洗い出す。**行番号の一覧は持たない**——編集のたびに壊れ、しかも壊れたことが一覧を見ても分からない（この項目は当初その形で書かれ、**作成した当日に陳腐化した**）
- **見出しで示した対象:** `01`「クロスレビューの指摘の扱い方」「Phase 1 補足」／`02`「共有/ローカル分離パターン」／`03`「メモリの推奨階層構造」／`05`「CLAUDE.md の階層構造パターン」「短命コンテキストファイルのライフサイクル」「構成の教訓」
- **⚠️ 公開のゲートにはしない:** これは特定の技術的事実を含まないため安全性の問題ではなく、**主張の一貫性＝内容品質**である。公開の条件を安全性のみに固定した決定に従い、公開後に扱う
- **進め方:** 各記述の出自（業務での実践／本リポジトリ自身での実践／一般知識）を分類し、**業務由来のものは「適用例」へ、本リポジトリ由来のものは「本ガイド執筆での実証」と出自を明示する**形へ揃える。`01:137` はこの形で処理済み（先行例）

### B. モデル選択の原則を `01` §2 に追記する

- **趣旨:** 「**新しい／速い／安いは、判断主体の作業への適性を意味しない**。世代交代が起きても、WHAT・設計・レビューは最上位の推論モデルを既定にする軸は変わらない」
- **書き方の制約:** 固有のモデル名は残さない。出すなら「◯◯時点の例」と明示して時限化する（記述が陳腐化したときに、原則ごと疑われないようにするため）
- **✅ 実施（2026-07-28・`v1.4.5`）:** `01` §2「用途別のモデル選択」を**原則＋時限付きの例**の2層に再構成し、`README` の同じ表にも時限化を適用した。あわせて「Sonnet 5：Opus級の知能」という**ベンダー表現の無検証転載**を除去（`RV6` の指摘の一部）。⚠️ **発火の契機は在庫の見返しではなく、人間が「Opus 5 に切り替えたのに表が 4.8 のままだ」と持ち込んだこと**——在庫は発火装置ではないという既知の欠陥の、もう1件の実例。
- **残（`RV6` の未処理分）:** `README` と `01` で同じ表が**重複している**（DRY 違反）。⚠️ 解消は読者の導線を変えるため、訂正と構造変更を1つの diff に混ぜない方針で分離した。

### C. 章ごとの裏取り状況を揃える

本ガイドは当初 Cursor を主に使っていた時期に書かれ、Claude Code native への移行後、**章によって再検証の進み具合が違う**。

| 章 | 裏取りの状況 |
|----|-------------|
| `01` | §2・§3.2・§3.5 は検証済み（うち §3.5 は一度誤りを訂正）。章全体は未検証 |
| `02` | **全面改訂済み**（2026-07-18）。主根拠だった主張が偽と判明し、結論を維持したまま根拠を差し替えた |
| `03` | §1（Skill の自動検出）とサブエージェント × Skills のみ検証済み。**章全体は未検証** |
| `04`・`05`・`06` | **章全体が未検証** |

- **進め方:** 推測で断定せず公式ドキュメントで裏取りし、確度ラベル（`[To Be Verified]` → `[Fact]`）を付けて更新する
- ⚠️ **「候補として挙げた分」と「章全体」を取り違えない。** 過去に「挙げた候補はすべて解消」と書いて、章まるごと未検証のまま完了扱いにした前例がある

### G. アジャイル/スクラム以外の「再現性」の文脈を補う

- **問題意識:** 本ガイドの背骨はアジャイル/スクラムの規律だが、**AI駆動開発の再現性を担保する文脈がそれだけとは限らない**。スクラム経験のない読者にも再現できる形になっているか、抜けている観点は何かを洗い出す
- **論点の例:** 再現性を支えるのはプロセス規律だけか／環境・設定の再現（`CLAUDE.md`・Skill・permissions の配備）／出力の検証手段（テスト・AC・裏取り）／モデルやツールが変わっても残る部分の切り分け
- **進め方:** 先に「何が足りないか」を洗い出してから、どの章に入れるかを決める

### Y. 状態記述（「未検証」「残: 〜」）が、それを偽にする作業の後も更新されない

- **内容 `[Fact]`:** 作業をして事実が変わったのに、その事実を偽にする**状態記述**が更新されずに残る、という失敗が**2回観測された**。
  - **1回目（2026-07-22②）:** `docs/design-decisions.md` に §6 を新設したのに、入口（README・ヘッダ）へ波及していなかった（2件）。
  - **2回目（2026-07-23③で検出）:** 2026-07-22② に `requirements-review` を実際に発火させ、その `[Fact]` を作業日誌に記録したのに、**`BACKLOG.md` の活性化表と項目 A は「初回運用未検証」「残: 初回運用での検証」のまま**だった。約1日、公開側に偽の状態記述が残った。
- **⚠️ 規約は既に存在する:** `CLAUDE.md`「変更を完了とする前に、その変更が壊した参照だけを確認する」は、確認対象として**「状態記述」を明示的に列挙している**。つまりこれは規約の不在ではなく**実行の失敗**であり、新しい規約を足すと二重管理になる。
- **論点（未決）:** ではなぜ発火しないのか。仮説 `[Judgment]` は、**この規約が「編集したファイル」を起点にしている**こと——`requirements-review` を発火させたセッションは `BACKLOG.md` を編集していないため、「編集したファイルが指している／を指している参照」という起点に引っかからない。**状態を変える作業は、ファイルを編集せずに状態記述を偽にしうる。**
- **⚠️ 起票先を design-decisions にしなかった理由:** 2026-07-22② の Try は「再発時に `docs/design-decisions.md` へ起票」としていたが、**まだ決定が無い**。決定のない項目を決定の記録へ書くと、本ガイドが §4 に記録済みの欠陥型（**実行対象のない規約が制御plane で無言のまま生き残る**）を再演する。決定が出た時点で移す。
- **⚠️ 対処を増やす前に:** X1（時間差の再点検）の適用範囲を広げる案が浮かぶが、**X1 は「前セッションで正本に書いた主張」を対象とする常設手順**であり、2セッション前の記述まで広げると毎回の負荷が上がる。**負荷を上げる対策は形骸化する**（「守るコストが高い規律は形骸化する」）。3回目の観測までは対策を決めない。
- 📌 **項目 S と隣接する。** 「状態ラベルが凡例の語彙内か」「✅ 行に公開成果物リンクがあるか」は**機械検査可能な形をしている**。⚠️ **ただし「だから検査器を置けば解決する」は項目 S で撤回した規範であり、ここでも前提にしない。**

### S. 機械検査できる規約が、人間の検出に依存している（2026-07-25 起票・同日に framing を訂正）

- **中核（観測に限定した形）`[Fact]`:** 本ガイドの規約はすべて散文で書かれ、**機械的に検査できるはずの規約でも、違反が人間に読まれるまで観測されない**。実測2件——①「ID は半角英数字」（`grep` 1行で検査可能）の違反を **2026-07-23 に人間が検出** ②版チェックの落ちを **2026-07-24 に人間が検出**。
  - ⚠️ **`docs/design-decisions.md` §4 の3欠陥型（実行対象のない規約／常時ロードされても素通り／発火の合図を持たない規律）が同一原因の現れではないか、は `[Judgment]` であって未検証。**
- **🔴 起票の経緯の訂正 `[Fact]`（本項目の最重要部分）:** 本項目は当初「**散文の規約に deterministic validator が無い**」という題で、「散文を選んだのは誤りで、正解は検査器を置くこと」という**規範**として起票された。**この framing は AI（Opus 4.8）の誤読の産物であり、同日中に撤回した。**
  - **誤読の内容:** 著者は [Unmesh Joshi "DSLs Enable Reliable Use of LLMs"](https://martinfowler.com/articles/llm-and-dsls.html)（Thoughtworks・2026年7月14日・martinfowler.com 掲載）を「**このセッションの反証根拠**」として持ち込んだ。この語は2通りに読める——**(a) 本ガイドの理念を否定する根拠** / **(b)「自分で動かして実証しろ」という理念に反した実例（＝理念を裏付ける失敗例）**。**AI は確認せずに (a) と決め、その前提で起票した。著者の意図は (b) だった。**
  - **⚠️ 帰結が重い:** (b) はガイドを**補強する**方向である。つまり **AI は、依頼されていない自己批判を公開リポジトリに書き込んだ**。しかも Zenn 記事が掲げた振る舞い（「AI が推測で埋めることを自分から拒否した」）の**逆**をやった——**分岐する語の指示対象を確認せず、推測で埋め、その上に起票した**。`CLAUDE.md`「判断に迷う場合のみ聞く」に照らせば、**1行聞けば済んだ場面だった**。→ 項目 D として起票。
  - ⚠️ **著者は Fowler 氏ではない。** martinfowler.com は Fowler 氏が編集する場であり、**著者は Unmesh Joshi**。混同すると、Zenn 記事の N6 で実際に修正した誤り（記者の要約を本人の発言として扱う／公表日を実施日として扱う）と同型になる。**「表示は実体の正本ではない」（項目 T）の追加実例**——読者は Fowler 氏の論考を期待し、実体は別著者である。
- **🔴 撤回した主張と、その反証 `[Fact]`:** 当初「2026-07-24 に版チェックが落ちた対処として散文1行（`dc009d3`）を足したのは誤りで、正解は検査器」と書いたが、**どちらが良いかは実測していない**。反証となる観測がある——**規約が破れた瞬間に `.md` を1行足すだけで、以後の振る舞いがその場で変わった**。parser や文法の書き換えを要する機構より速い。**「検査器のほうが正しい」は規範であって観測ではなく、確度ラベルを付けずに書いていた。**
  - 📌 **一般化 `[Judgment]`:** Joshi の validator が検査するのは**生成された成果物**、本ガイドの規約が律したいのは**エージェントの振る舞い**。**同じ「検査」の語で括ったこと自体が誤りだった可能性がある。**
- **`[Fact]` 閉じた語彙は既に4つあり、どれも機械検査可能なのに検査されていない:**

  | proto-DSL | 閉じた語彙 | 可能な検査 |
  |---|---|---|
  | 確度ラベル（`01` §2） | `[Fact]` / `[Assumption]` / `[Judgment]` / `[Inference]` | 語彙外ラベルの検出 |
  | 活性化表の状態 | ✅ / 🟡 / ⬜ / ⏸（凡例あり） | 凡例外の状態・✅ 行に公開成果物リンクが無い行（→ 項目 Y） |
  | ID の文字種（`CLAUDE.md`） | 半角英数字 | **丸数字の検出＝`grep` 1行** |
  | 版（`design-decisions` §5） | MAJOR / MINOR / PATCH | ガイド本体を触ったコミットのタグ到達性（適用範囲は §5「『公開物』を目的別に2つの適用範囲へ分ける」） |

  📌 **この表は観測であって、処方ではない。** 「検査可能なのに検査していない」は事実だが、**「だから検査器を置くべき」は別の主張**であり、上記のとおり未検証。⚠️ **当初この表に「規約を足した回数ではなく検査器を置いた回数を数えるべきだった」と結論を付けていたが、撤回した。**
- **📌 検査点は既に1つ存在している `[Fact]`（2026-07-25 追記）:** 機械的な validator は撤回したが、**commit 前の人間の diff レビュー**（`docs/design-decisions.md` §2「commit / push は人間が実行する」が作る猶予）が、実際に検査点として機能した。2026-07-25、**AI の申告と実体のズレを3件検出したのはすべてこの経路**（①「公開状態で残る」が未 push だった ②機微語を「書かない」と言いながら書いていた ③状態報告が実体とズレる）。**機械的ではないが、AI の自己申告に依存しない**という点で X1 と同じ性質を持つ。⚠️ **ただし「規約が検出した」ではなく「規約が猶予を作り、人間が diff を読んで検出した」——機会の提供と検出の実行は別物であり、検出は人間側にある**（K-6：AI の自己申告からは1件も出ていない）。
- **⚠️ 適用範囲を限定しないと過剰適用になる（重要）:**
  - **Joshi の対象はコード生成**であり、validator が成立するのは**正しさが機械検査可能な成果物**だから。本リポジトリの主成果物は**方法論の散文と判断の記録**で、「この判断は妥当だったか」に parser は原理的に存在しない。**効くのは機械的な規約の層に限る。**
  - **丸ごと適用するのは、項目 T が扱う誤り（フレームワークの存在から実態を推論する）の鏡像である。**
  - **"Design Is Discovered Through Implementation"（"A specification is at best a starting hypothesis"）は反証ではない**——項目 E・PBR の議論と**同じ観測**であり、反証として数えると誤読になる。⚠️ ただし `04` / `06` の WHAT-first の framing には**圧力として残る**（要検査。要件の反転ではない）。
  - 📌 **記事は「自然言語が本質的に不十分」とは主張していない**（逐語で確認済み）。**1回目の取得要約はそう述べたが、これは要約モデルの推論だった**——取得結果を1回で信じない運用が効いた実例（項目 T の系）。
- **⚠️ 対処は未確定 `[Assumption]`:** hook（`settings.json`）で落とす案はありうるが、**このリポジトリには Windows で設定ベースの防御が破れた実績がある**（読取拒否が Bash 経由で抜け、サンドボックス設定が機能しなかった。`CLAUDE.md`「機微なものは設定で守らず、置き場所で守る」）。**「hook を置けば解決」は推測であり、1本作って実際に違反を落とすまで確定させない。**
- **論点（未決）:** ①**そもそも検査器を置くべきかどうか**（撤回した規範。散文1行の即時性という反証がある）②置くなら最も安いのは丸数字 `grep`、最も価値が高いのは版タグ到達性 ③hook が Windows で機能するか ④検査器を増やすこと自体が膨張パターン（M5）にならないか——「検査器は人間のコストを増やさない」という点で従来の規約追加とは性質が違う、が要検証 ⑤このリポジトリは「コードを持たない」ことを前提に設計されており、検査スクリプトを持つと `01`〜`06` の品質ゲートと誤読される恐れがある（項目 T の実例2＝lint 設定の誤認と同型）。
- **⚠️ 当該記事を「書いた≠発火した」の外部事例として起票する場合の歯止め（未起票・2026-07-25 に判断のみ記録）:** 著者の中核の観測——**記事にはその方法論をエージェント環境で走らせた記録・失敗の記録・確度の区別が無い**——は成立する。**しかし、著者が壁打ちに使った AI が乗せた次の3点は取り込まない:**
  - **①「著者が動かしていない」は不在からの推論。** `docs/design-decisions.md` §4 が**この欠陥型を名指しで記録している**（「不在から挙動を推論していた——否定形の断定は、実行されない限り反証されない」）。**書かれていないことは、やっていない証拠ではない**（当該著者は自身の DSL では動かしている）。
  - **②「腐っている」「手を動かさない」は人物評**であり、確度ラベルを付けられない。**観測（実証の記録が無い）からは導けない。**
  - **③「martinfowler.com は提灯記事の場になった」は `[Assumption]`。** 掲載が編集判断である可能性を排除していない。⚠️ **ここを断定すると、表示の変化から実体の変化を推論するという項目 T の誤りそのものを踏む。**
  - 📌 **成立するのは「表示（サイト名）は著者の正本ではない」まで**（項目 T）。**中核だけを書けば良い材料になり、3点を外さずに取り込むと §4 の欠陥型を公開物に入れることになる。**
- **⚠️ 位置は未検討。** 起票のみで、優先順位は次の PBR で決める（起票と優先順位付けは別の作業＝項目 Q）。**末尾に置いたのは AI の既定であって判断ではない**（項目 Y と同じ扱い）。

### D. AI が分岐する指示を検出しても確認せず、推測で埋めて公開物まで届く（2026-07-25 起票）

- **中核 `[Fact]`:** 2026-07-25、著者のプロンプトに「**このセッションの反証根拠になると思ったので意見して**」とあり、「**何に対する反証か**」が書かれていなかった。**2通りに読める**（ガイドを否定する根拠／ガイドの理念に反した失敗例）にもかかわらず、**AI（Opus 4.8）は確認せずに一方を選び、その前提で意見を組み立て、公開リポジトリに項目を起票した。選んだ側が誤りだった**（→ 項目 S）。
- **なぜ重いか:** 誤りが**対話の中で止まらず、公開物に到達した**。しかも選んだ読みは「ガイドへの批判」側で、**著者が求めていない自己批判を公開リポジトリに書き込んだ**。⚠️ **批判的な出力は「検査済みに見える」ため、人間側の検査も弱まる。**
- **⚠️ 項目 E の裏面である `[Judgment]`:** 項目 E は「**人間の意図を伝える定位置が無く、後出しで裁定が覆る**」＝機構の欠落を扱う。本項目は「**AI が分岐を検出できたのに聞かなかった**」＝実行の失敗であり、**機構を足しても直らない可能性がある**（項目 E の「人間が最初に全部言え」に倒すと形骸化する、という判断と衝突しないよう分けて起票した）。
- **⚠️ 規約は既にある:** `CLAUDE.md`「**情報が揃っている作業でユーザーに確認を求めない。判断に迷う場合のみ聞く**」。**迷うべき場面だったのに迷わなかった**のであり、規約の不在ではない（項目 Y と同じ構造＝新しい規約を足すと二重管理）。
- **📌 皮肉として記録する `[Fact]`:** 公開済みの Zenn 記事は「**AI が『推測で埋めること』を自分から拒否した**」を主題にしている。**その振る舞いの逆を、記事公開の2日後に、記事を産んだ当のリポジトリでやった。**
- **論点（未決）:** ①「指示の語が2通りに読める」を AI 側が検出したときの扱い（**聞く／両方の読みで出力する／推測を明示して進む**のどれを既定にするか）②公開物への書き込み**前**に1回止まる形が要るか——⚠️ ただし**止める規約を足すと全ての書き込みが遅くなる**ため、適用範囲を「**前提が1通りに定まらない場合のみ**」に限る必要がある ③本項目と項目 E・項目 R（人間側の書き方）の関係整理。
- **⚠️ 観測は1件。** 対策は2回目を見てから決める（M5・「1回で断定しない」）。**位置は未検討**（末尾は AI の既定）。

### F. 分離は内容に効くが、内容に付随するメタデータには効かない（2026-07-25 起票・優先度はそれなりに高い）

- **中核 `[Judgment]`:** 非公開にすべきものを**別リポジトリに置く**という設計（[`docs/design-decisions.md`](docs/design-decisions.md) §1「非公開ファイルは『git 管理外』ではなく『別リポジトリ』に置く」）は、**本文の内容には効くが、公開する内容に付随するメタデータには効かない**。メタデータは公開物に貼り付いており、別リポジトリへ動かせない。
- **`[Fact]` 何が残るか:** commit の author/committer date、annotated tag の tagger date、GitHub Release の公開日時は、**リポジトリオブジェクトまたはプラットフォーム側に埋まり、別リポジトリへ移せない**。タイムゾーンのオフセットも含まれる。**単体では断片だが、`git log` は時系列の全件を機械可読で返すため、累積したパターンを読み出す作業は誰でも一瞬で行える。**
- **⚠️ `CLAUDE.md` が名指しで警戒している構造が、本文ではなく git メタデータ側で成立していた:** 「個々の記述が許容範囲でも、**時系列で累積すると傾向が像を結ぶ**」。**起票時点で、この論点は `.md` のどこにも記録が無かった** `[Fact]`（`タイムスタンプ`／`コミット時刻`／`稼働時刻` 等で全文検索し0件）。
- **⚠️ 同型を1回踏んでいる `[Fact]`:** 「意図していなかったセッション中のキャプチャの commit をガードする規則を追加」＝**内容の意図しない混入**。`git add -A` を禁じた規約は**内容しか守っておらず**、本項目はその**メタデータ版の第2実例**。
- **🔴 著者自身の裁定は、この公開ファイルには書かない（2026-07-25 の判断）:** 本項目には当初、著者がこの論点をどう裁定したかを**理由つきで**書いていたが、**その理由自体が生活・稼働の記録**であり、分離の判定基準（「**著者の生活・稼働の記録か、内容についての判断の記録か**」）に照らして非公開側に属する。**削除した。**
  - 📌 **この失敗そのものが本項目の要点である `[Fact]`:** **メタデータの漏れを扱う項目の本文で、メタデータより濃い情報を公開側に書こうとした。** 対処が問題を悪化させる型。⚠️ **原因は、人間が「AI の判断を助けるために」渡した私的な背景を、AI が「裁定の根拠」として公開物へ転記したこと**——**判断のための入力と、公開のための素材は別物**である（→ 項目 D と同じ、確認を挟まずに進む型）。
  - 📌 **既存の設計と整合する:** 作業日誌の置き場所・対話での呼称は、いずれも**著者環境の値であって公開本文には持ち込まない**と決めている。**この論点の裁定も同じ扱いにする**のが一貫した処理。
- **⚠️ 却下した対処（明示記録）:** **commit date の書き換え（`--date` / `GIT_COMMITTER_DATE`）は使わない。** 本ガイドの中核は「記録は証拠」「確度を偽らない」であり、日時の偽装は §4 が扱う型（**表示と実体の乖離を自分で作る**）そのもの。**項目 I・項目 J と同じく、暗黙却下にせず記録する。**
- **⚠️ 精密化しておくこと（対処を検討する人が誤らないために）:**
  - **抜け穴の正体は特定の時刻ではなく、「commit という、メタデータが必ず付く公開行為」。** ある時刻を避けても、何らかの時刻は必ず刻まれる。
  - **「作業中に commit し、後で push する」は無効。** push 時刻はリポジトリオブジェクトに入らないが、**commit date は入る**。有効なのは「**作業と commit を切り離し、commit を晒したい時間帯に寄せる**」だけ。
  - **🔴 「稼働の時間帯は `README`「書かないもの」の中核の外側だから軽い」と当初書いたが、これは誤りであり撤回した。** 時間帯・空白期間・パターンの乱れは、**それ自体が機微なのではなく、`README`「書かないもの」が守っている領域への間接的な経路になりうる**。**「中核の外側」ではなく「中核へ通じる裏口」であり、危険度を下げて評価してはいけない。** ⚠️ **どのような推定が成り立つかは、ここには書かない**——**経路を明示すること自体が経路になる**（`README`「保管場所も書かない——ポインタ自体が経路になるため」と同じ扱い）。
- **📌 本文への追記候補としての価値（本項目の残る主題）`[Judgment]`:** **この論点は、読者の置かれた状況によって裁定が変わる。** ある人には抜け穴として残り、ある人には害がない。→ **[`docs/design-decisions.md`](docs/design-decisions.md) §1「非公開ファイルは『git 管理外』ではなく『別リポジトリ』に置く」に、適用範囲の限界として付記する候補**——「**内容は分離できるが、内容に付随するメタデータは分離できない**」。
  - ⚠️ **書くのは判断材料であって、著者の裁定ではない。** そして**判断材料の書き方にも制約がある**——「**どういう人が危ないか**」を具体的に列挙すると、**列挙自体が推定の手引きになる**。**構造（分離できないという事実）だけを渡し、当てはめは読者に任せる。**
- **⚠️ 位置は未検討**（末尾は AI の既定）。**優先度は著者判断で「それなりに高い」**——次の PBR で反映する。

### Z. PBR で並び替えと追加を混ぜると、diff で「移動」と「新規」が区別できず人間のレビューが盲目化する（2026-07-25 起票・人間が持ち込んだ盲点）

- **中核 `[Judgment]`（著者が言語化）:** PBR で**大幅な並び替えと項目追加が同一バッチで起きると、git diff 上で「どれが移動でどれが新規か」を人間が判別できなくなる**。AI 速度のバッチ操作と、人間の diff レビューの限界とのミスマッチ。**まだ顕在化していないが、AI 側からは構造的に気づけない視点**——人間のレビュー認知限界を AI はモデル化できない（項目 O「AI は並行変更を検知できない」の系）。
- **既存の対処では埋まらない `[Fact]`:** 2026-07-24 の PBR は node スクリプトでディスク直接操作し**転記ミスをゼロにした**（K1）が、それは「移動が正確か」の問題。「**移動と追加を人間が diff で区別できるか**」は別問題で、未対処。
- **対策候補 `[Judgment]`:** PBR を**「並び替えのみのコミット」→「追加のみのコミット」の2段に分ける**。diff が「純粋な移動」と「純粋な追加」に割れ、**commit 前の diff レビュー（検査点。`docs/design-decisions.md` §2）が盲目化しない**。項目 S の「commit 前の diff レビューが検査点」を守る operation hygiene であり、**K8「削除は参照の付け替えとセット」と同型**（操作を、レビュー可能な単位に分割する）。
- **⚠️ 項目 U（速度は検出窓を圧縮する）の *operation* 版。** U は「書いた内容の検査」、本項は「**操作そのものの検査可能性**」を扱う。
- 📌 **本項の起票は純粋な追加（並び替えなし）なので diff はクリーン**——対策の実演になっている。
- **⚠️ 位置は未 triage**（末尾は AI の既定）。優先度は次の PBR で決める。

### W. モデル提供元の公式ガイダンスと本ガイドは、評価軸が別物である（2026-07-26 起票／2026-07-27 立脚点を変更・人間が持ち込んだ盲点）

- **中核 `[Fact]`（一次ソース逐語）:** Claude Opus 5 の公式プロンプティングガイドは「If your prompt contains explicit verification instructions ("include a final verification step for any non-trivial task," "use a subagent to verify"), **remove them: instructions like these cause over-verification on Claude Opus 5, and removing them reduces wasted tokens with no loss in quality.** The same applies to legacy harness scaffolding that adds separate verification steps.」と述べ、さらに「Avoid instructing re-checks it already performs」と続ける（[Prompting Claude Opus 5](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5)・確認日 2026-07-26）。
- **🔴 適用範囲は検証指示にとどまらない `[Fact]`（一次ソース逐語）:** 公式ブログは「**We removed over 80% of Claude Code's system prompt for models like Claude Opus 5 and Claude Fable 5 with no measurable loss on our coding evaluations.**」と述べ、Then→Now の対比として「**Let Claude use judgement**」（明示的なルールと制約を与える → 不要なガードレールを外す）「**Claude now automatically saves memories that are relevant to the work**」（`CLAUDE.md` への手動保存 → auto-memory）を挙げる（Thariq Shihipar, [The New Rules of Context Engineering for Claude 5 Generation Models](https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models)・Anthropic・2026-07-24 公開・確認日 2026-07-27）。⇒ **当たっているのは検証指示だけでなく、規約を積んだ常時ロードの制御plane そのものである**（実測: 本リポジトリの `CLAUDE.md` は 22,242 バイト／115行）。
- **なぜ本ガイドに当たるか:** 本ガイドの制御plane は**検証指示の積み上げで規律を実装している**（時間差の再点検・前提の再点検・「実測してから発話する」）。公式の助言をそのまま適用すると、**これらは「消すべき scaffolding」に分類される。**
- **⭐ 立脚点 `[Judgment]`（2026-07-27 に変更）:** これは「公式への反論」ではなく**評価軸の相違**である。公式の "no measurable loss" が測っているのは **coding evals＝モデル出力の品質**。本ガイドの規約が守るのは ①**人間が裁定した証跡が残ること** ②**人間が状態を頭の中に置かずに済むこと**——**どちらも coding evals では測れない。受益者が違う。**
  - ⇒ **「公式がやめろと言った」は撤回の理由にならない。** だが同時に、**これは「本ガイドが正しい」の根拠にもならない**——別の軸で測っていない、というだけである。
- **📌 立脚点②の機序 `[Assumption]`（本ガイド自身の運用から）:** 提案→レビューを細かいステップで回すと、**各時点で参照すべき対象が小さく保たれる。** 深い思考や丁寧な指示の記述は行われるが、**保持を要求される対象が少ないため、作業記憶は空いたままになる。** ⇒ **「よく考えること」と「頭に置き続けること」は別のコストである。**
  - ⚠️ **`[Assumption]` である理由:** これは運用者の自己報告であり、認知負荷を測定したものではない。**ただし確かめれば決着する領域**であり、[`01`](01_ai-driven-dev-strategy.md) §2 の軸では `[Judgment]`（確かめても決着しない）ではない。
  - ⭐ **論点③（人間側の受益をどう測るか）への寄与:** 測る対象の候補が1つ具体化した——**「同時に参照すべき対象の数」**。思考の深さでも所要時間でもなく、**保持を要求される対象の数**を数える。
  - 📌 **同じ運用（セッションを小さく切る）を [`01`](01_ai-driven-dev-strategy.md) §3.2 は Token・cold write の理由で勧めている。** ⇒ **同一の運用が、まったく別の受益者に効いている**——**受益者が違うので同じ評価軸に乗らない**という本項の中核が、**本ガイド自身の運用に現れた形である。**
- **🔴 それでも無視できない理由 `[Judgment]`:** ①**コストは実測で出ている**——1セッションで往復回数×文脈サイズが支配項という測定があり、検証の往復はその支配項側に乗る ②公式は「no loss in quality」と断定しており、**本ガイドは「検証を足せば質が上がる」を実測で確認していない**（付け足した規約が効いたことの測定が無い＝項目 Y／`CLAUDE.md`「効果未検証」と同じ穴）③**軸が違うと主張する以上、その軸で測る手段を持たないと同じ穴に落ちる**——公式は測定に基づいて "no measurable loss" と言っており、こちらは「人間側の受益」を測っていない。
- **📌 一部はブログより前に、別の理由で退けている:** `Memory in CLAUDE.md → Auto-memory` について、`CLAUDE.md`「正本は git 追跡ファイル、AIのメモリは per-machine キャッシュ」は **`git clone` に付いてこない**ことを理由に既に退けている。**ブログへの反応ではなく、独立に到達した判断である**（一致は結論ではなく論拠を見て判定すること）。
- **論点（未決）:** ①本ガイドの検証規律を「**AI の自己検証**（削れる）」と「**人間の裁定のための証跡**（残す）」に分類し切れるか ②分類した結果、実際に削れるものが出るか ③**人間側の受益を測る方法があるか**——無ければ ①② は主張のまま残る ④`01`／`CLAUDE.md` のどちらに書くか（**方法論として `01`、運用として `CLAUDE.md`** の二重管理を避ける）。
- **📌 併せて拾った一次情報（個別に裁定する）:**
  - **effort は5段階 `[Fact]`（`low`/`medium`/`high`（既定）/`xhigh`/`max`）** — 出典 [Effort](https://platform.claude.com/docs/en/build-with-claude/effort)・[Model configuration](https://code.claude.com/docs/en/model-config)（確認日 2026-07-26）⇒ `01` §2 のモデル・設定の記述に効く。⚠️ **`max` は settings に永続化できず session-only**＝「毎回人間が選ぶ」運用になるため、本ガイドの設計（記憶と注意力を前提から外す）と衝突する。
  - **⭐ effort を会話の途中で変えるとプロンプトキャッシュのプレフィックスが無効になる `[Fact]`**（「changing it between requests does not preserve cached prefixes … pick an effort level at the start and keep it constant」）⇒ **`01` §2 の Token コスト節（RV8 と同じ節）に効く一次ソース**。「タスクごとに設定を切り替える」助言を書く場合、**キャッシュを壊す副作用を併記しないと読者が損をする。**
  - **要約ページと正本ページの階層を読み違えた実例 `[Fact]`（本項の起票時に発生）:** prompting ページ（要約）だけを読んで「一次ソースで確定した」と作業日誌に書いたが、そのページ自身が「See Effort for the full recommendations」と正本を名指しており、`max` が漏れていた。**人間が別ページを持ち込んで初めて露見。** ⇒ **項目 T「表示は実体の正本ではない」の外部実例**として使える（公式ドキュメントでも「そのページが正本か」を見る必要がある）。
  - **subagent の過剰委譲 `[Fact]`:** 「Claude Opus 5 delegates to subagents more readily than prior models … it multiplies cost and time when applied to small tasks」⇒ `01` の並列調査の記述（**RV13 と同根**。総 Token は増えうる）を**一次ソースで裏付けられる**。
  - **1M token context がデフォルト兼上限 `[Fact]`** ⇒ 文脈量の前提に効く。
- **⚠️ 位置は未 triage**（末尾は AI の既定）。**`01` はガイド本体なので、本文へ反映する時点で版チェックが発火する。**

### AA. 実演・登壇は非可逆な開示であり、「可逆な措置を先に打つ」規律が機能しない（2026-07-27 起票・人間が持ち込んだ盲点）

- **中核 `[Judgment]`:** 本ガイドの開示に関する規律は、露出が判明した後に**可逆な措置から順に打つ**ことを前提にしている（[`docs/design-decisions.md`](docs/design-decisions.md)「保護は『保管場所』ではなく『持ち出し経路』で切れる」）。**ライブの実演・登壇・配信ではこの前提が成立しない**——配信・録画・撮影は事後に取り消せず、**検査のタイミングが露出より後になる。**
- **🔴 本ガイド固有の事情:** 再開手順は「**作業日誌『次セッションの開始点』を読む**」から始まる。⇒ **「セッションの開始」を実演すると、定義上、作業日誌が画面に出る。** 作業日誌は公開しない設計（[`docs/design-decisions.md`](docs/design-decisions.md)「作業日誌は公開しない」）であり、**実演の導線と公開設計が正面から衝突する。**
- **⚠️ 行単位の注意では防げない:** スクロールする画面に対して1行ずつの判定は行えない。**検査の単位と危険の単位が一致しない**——上記「持ち出し経路」の項と同じ構造である。
- **⚠️ 最も見せたいものが、最も出せない:** この方法論の説得力は**実際に積み上がった状態に対して Check Action / X1 が発火する**ところにある。無害化したデータでは**仕組みは示せても効果が示せない。**
- **論点（未決）:** ①デモ専用の作業日誌（公開可能な内容だけの STATE）を用意するか ②作業日誌を映さない導線（`README` → `CLAUDE.md` → `BACKLOG`）で構成するか ③公開本文は「各自が自分の作業日誌を持つ」と書いている——**実演で著者の作業日誌を見せることは、その記述と整合するのか**
- **⚠️ 位置は未 triage**（末尾は AI の既定）。

### AB. 検証済み環境（OS・シェル）が明文化されておらず、変わったことを申告する定位置も合図も無い（2026-07-27 起票・人間が持ち込んだ盲点）

- **問題:** 本ガイドは**どの OS・シェルで検証されたかを書いていない**。読者にも、著者自身の別マシンにも、「この手順はどの環境で確かめられたか」が伝わらない。⚠️ **加えて、環境が変わったときに人間がそれを申告する定位置も、AI がそれを問う合図も無い。**
- **実測 `[Fact]`（2026-07-27・本リポジトリ全 `.md`）:** OS/シェルへの言及は**4箇所のみで、すべて条件付きで明記されている**——[`04`](04_spec-kit-integration.md) は Linux/macOS と Windows(PowerShell) を併記、[`01`](01_ai-driven-dev-strategy.md) §2 と [`CLAUDE.md`](CLAUDE.md) の「サンドボックス設定は Windows では機能しなかった」、項目 S の「hook が Windows で機能するか」も同様。⇒ **公開本文に隠れた OS 前提は見つからなかった。**
- **🔴 それでも問題が残る理由:** [`setup-claude-code.md`](setup-claude-code.md) には **OS への言及が1つも無い**。これは「OS 非依存である」ことの表明ではなく、**どの環境で検証したかを書いていない**だけである。⚠️ **「書いていない」は「どこでも動く」ではない**——本ガイドが別の文脈で確認した「**変化が無かったは、安全だったではなく測れていない**」（[`docs/design-decisions.md`](docs/design-decisions.md)「保護は『保管場所』ではなく『持ち出し経路』で切れる」）と同じ型である。
- **前提 `[Fact]`（著者申告）:** 現在の運用は単一 OS に寄っているが、**別 OS へ移る見込みが現実にある。** ⇒ この項目は仮想的な懸念ではない。
- **論点（未決）:** ①検証済み環境を書く定位置をどこに置くか（`setup-claude-code.md` の冒頭か `README` か）②「環境が変わった」を申告する合図をどう作るか——**人間の報告義務にすると形骸化する**ので（項目 O が確認済み）、**再開手順の側で問う形**が候補 ③手順・コマンド例を OS 非依存の書き方へ寄せるか、両対応で併記するか（`04` に併記の前例がある）
- **🆕 実装案（2026-07-30 追記・未裁定）— 環境の記述は「配布物」ではなく「マシンごとの実測値」である。**
  - **動機 `[Fact]`（本ガイド自身で観測・2026-07-30）:** AI が**実行環境で使えないものを使えると想定して実行し、失敗してから代替へ回る**という往復が実際に起きた（`python` を実行し「使えませんでした」で `Edit` へ切り替えた）。⇒ **論点②の「合図」以前に、AI が参照できる環境の記述そのものが無い。**
  - **案:** 環境の実測値を**マシン単位のファイル**に置き、再開手順から参照する。**別マシンにはそのファイルが無い**ため、init（環境の測り直し）が自然に走る。
  - **⚠️ `.gitignore` による除外で実装しない `[Judgment]`:** [`CLAUDE.md`](CLAUDE.md) は同じ設計を一度捨てている——「`.gitignore` による除外で実装していたが、**変更を差分で確認できなくなり、AI による書き戻しがレビューの外に置かれた**」。**環境ファイルは AI が書き戻す対象**なので、除外すると**誤った環境情報を書き込んでもレビューできない。**
  - ⇒ **代わりに、追跡したままファイル名にマシン識別子を持たせる**（`env-<識別子>.md`）。**別マシンには自分の名前のファイルが無いので init は同じように走り、かつ差分で追える。** 📌 **副産物として複数マシンの環境が並んで残る**——複数台で運用する場合はこれ自体が利得になる。
  - **📌 「正本は git 追跡ファイル」原則との衝突は無い。** 環境は**マシン固有の事実**なので per-machine が正しく、これは例外ではなく**原則の正しい適用**である（耐久的な知識ではない）。⇒ **原則の適用範囲を1文添えるべき**という論点が派生する。
- **📌 項目 O との違い:** **O は「セッション中に人間が並行して環境を変える」話**で、対策は AI 側の規律（実測・確認してから発話する）に置いた。**本項は「セッションを跨いだ実行環境そのものの前提」で、そもそも前提が明文化されていない。** ⇒ **O の対策は本項に効かない**——問うべき対象が書かれていないものは、実測しようがない。
- **⚠️ 位置は未 triage**（末尾は AI の既定）。**`README`・`setup-claude-code.md` に触れる場合は版チェックの扱いを確認する**（`setup-claude-code.md` はガイド本体か否かが未判定）。

---

### AC. Skill の「出所」が2系統しか書かれておらず、読者は Skill＝自作と読む（2026-07-28 起票・人間が持ち込んだ盲点）

- **問題:** [`03`](03_agent-skills-knowhow.md) の配置先表は **個人用（`~/.claude/skills/`）とプロジェクト用（`.claude/skills/`）の2系統**を挙げ、「このSkillは別プロジェクトでも使えるか／このプロジェクト固有か」で振り分ける判断表を持つ。⇒ **表としては完結して見えるが、これは「自作した Skill をどこに置くか」の2系統であり、Skill の出所そのものは4系統ある。**
- **実測 `[Fact]`（[Claude Code 公式「Extend Claude with skills」](https://code.claude.com/docs/en/skills)・確認日 2026-07-28）:** 出所は **①同梱（bundled）②個人用 ③プロジェクト用 ④プラグイン** の4系統。**同梱 Skill は全セッションで利用可能**で（`/doctor`・`/code-review`・`/debug`・`/loop` 等）、`disableBundledSkills` で無効化できる。プラグイン Skill は `<plugin>/skills/<name>/SKILL.md` に置かれ、plugin を有効にした場所で使える。
- **🔴 なぜ問題か:** 本ガイドが書いている2系統はいずれも**自作前提**である。⇒ **「Skill を使う」＝「Skill を作る」と読める構造**になっており、**既に手元にあるもの（同梱）と、取ってくる経路（プラグイン）が読者に渡っていない。** ⚠️ **「提供範囲の外だから書かない」とは言えない**——`03` は既に配置先の表を持って**この領域に踏み込んでいる**。**踏み込んだ表が不完全である**という形の欠陥である。
- **⚠️ 読者が誤認しうる `[Assumption]`:** Claude Code を導入した利用者が、Skill の出所を「自作のみ」または逆に「どこかから取得するもの」と誤認しうる。**本ガイドはこれを測っていない**（読者調査をしていない）。
- **論点（未決）:** ①4系統を `03` に書くか、それとも**公式ドキュメントへの導線1本**に留めるか——⚠️ **仕様の複製は腐る**（[`README`](README.md) が「README と手順書に同じ列挙を置いて README 側が腐った」を自ら記録している）②同梱の `/code-review` が**本ガイドのクロスレビュー（項目 H）の代替になるか**——⚠️ **ならないと考えている**（`/code-review` はコード差分向け、本ガイドの対象は Markdown の方法論本文）が、**試していない** `[Assumption]` ③`README` §「モデル選択の指針」の段階2 に導線を張るか（現状は張っていない）
- **📌 項目 H との違い:** **H は「本ガイドの方法論を Skill として実装する」**（作る側）。**本項は「Skill がどこから来るかを読者に渡していない」**（説明する側）。⇒ **H が完了しても本項は残る。**
- **⚠️ 位置は未 triage**（末尾は AI の既定）。**`03` に触れればガイド本体なので版チェックが発火する。**

---

### AD. AI の編集を diff だけでレビューすると、距離のある矛盾は原理的に見えない（2026-07-28 起票）

- **問題:** AI に文書を編集させ、**その diff をレビューして採否を決める**のは自然な運用だが、**diff が構造的に写さない誤りがある**。⚠️ **これはレビュアーの注意力の問題ではない**——見えないものは、注意を強めても見えない。
- **2種類ある `[Fact]`（本ガイド自身で観測・2026-07-28）:**
  - **① 距離** — 矛盾する相手が文脈行の外にある。実例: 誤った主張を [`README`](README.md) の 103 行目に書き、**それを否定する記述は 126 行目**（23行先）にあった。`git diff` の既定文脈は前後3行で、**両者が同じ画面に載ることはない。**
  - **② 読み方** — diff は「**行**」を見せるが、**散文としての通読**をさせない。実例: 段階の呼び方が `①②③` と「段階1/3」で混在していたのは**変更行の中**だったが、`+`/`-` の対として読むと違和感が出ず、**段落として読んで初めて分かった。**
- **🔴 検査は動いていた。** 参照検査の `grep` は当該行を**ヒットさせていた**。AI はそれを読んで「壊れていない」と判定して通し、**その同じ行に誤った主張を追加した**——読んだのは前後8行で、23行先は視界の外だった。⇒ **参照検査は片方向である**: 「既存の参照が壊れたか」は見るが、「**自分の追記が既存記述と矛盾するか**」は見ない。
- **⚠️ 検査コストが人間側へ移る:** AI が diff だけを提示すると、**矛盾を見つけるために前後数十行を俯瞰する作業が人間の側に残る**。実際、本ガイドではこの発見自体が「著者が俯瞰して読み直してようやく気づいた」経路で出た。⇒ **項目 W と同型**（AI 側の省力が人間側の負荷として現れる）。
- **論点（未決）:** ①**対処を `01` §2 のクロスレビュー運用に接続するか**——「独立性を確保する3段階」は**レビュアーの独立性**を扱っているが、**レビュー対象の提示形式（diff か、節か）**は扱っていない。⇒ **同じ節に「何を見せるか」の軸を足すかは未判定** ②**規約「変更が壊した参照だけを確認する」を広げるか**——⚠️ **広げない方向を推す** `[Judgment]`。同規約は「全体の整合性を通す作業ではない」と**意図的に範囲を絞っており**、広げると毎回の編集が重くなる。**代わりに「`grep` のヒットを『壊れていない』で閉じない」という、既に走らせている検査の出力を捨てない形が安い** ③この一般化がどこまで効くか——**本ガイドは Markdown の散文で観測した** `[Assumption]`。コード diff でも同じかは測っていない（テストが矛盾を捕まえる分、効き方が違う可能性がある）
- **⚠️ `01` に触れればガイド本体なので版チェックが発火する。**

---

### AE. 深い思考・調査は、セッションの中では生産されない（2026-07-30 起票）

- **問題:** 本ガイドは「セッションをスプリントとして扱う」（[`01`](01_ai-driven-dev-strategy.md) §3.6）が、**スプリントの中で行われる活動しか記述していない**。⇒ **枠組みに「セッションの外」が無い。**
- **観測 `[Fact]`（本ガイド自身の運用・2026-07-30）:** 著者が**セッションの外**で言語化した論点が、**一度に14件**まとめて持ち込まれた。内容は個別の思いつきではなく、**設計上の欠陥の指摘・実装案・出典の提示**を含んでいた。⇒ **セッション中に出ていなかったもので、かつセッション中には出せなかったもの。**
- **著者の言語化 `[Fact]`（申告）:** 「**セッション側に集中しているとこういう深い思考、調査ができない。**」
- **🔴 なぜ規律の問題になるか:** セッション中の AI は**その場の作業に最適化された相手**であり、**「この設計は根本から違うのでは」という種類の問いを引き出す構造を持たない**。⚠️ **本ガイドの既存の検査（Check Action・確度ラベル・レトロ）はすべてセッション内の装置**であって、**セッション外で生産されたものを取りこぼさない仕組みではない。**
- **⚠️ 隣接する誤った当てはめ `[Judgment]`:** スクラムの「バッファの確保」（就業時間の20〜30%を余裕として計画する）は**一見当てはまるが、目的が違う**——あれは**見積りの不確実性**への備えであり、ここで必要なのは**思考の場所の確保**である。⇒ **対応するのはスプリント内のバッファではなく、「スプリント外の活動」**（プロダクトバックログリファインメントが専用の時間として置かれているのと同じ位置づけ）。
- **論点（未決）:** ①セッション外で出たものを受ける**定位置**を作るか（現状は次セッション冒頭に口頭で流し込むしかない）②`01` §3.6 に「スプリント外」の節を足すか——⚠️ **足すとガイド本体なので版チェックが発火する** ③**AI 側から取りに行けるか**——📌 **本ガイドは既に「取りに行かないと落ちる」と記録しているが、取りに行く合図は持っていない**（項目 O・AB と同じ構造の欠落）④**測れるか** `[Assumption]`——「セッション外で生産された論点の件数」は数えられるが、**それが多いことは良いことなのか悪いことなのか**（＝セッション内で出せていない、と読むべきか）が決まっていない
- **📌 項目 AB との違い:** **AB は「環境が変わったことを申告する合図が無い」**（対象は環境という**事実**）。**本項は「思考が生産される場所が枠組みの外にある」**（対象は**論点**）。⇒ **AB の対策（再開手順で問う）は本項には効きにくい**——事実は問えば答えが返るが、**論点は問われて出てくるものではない。**
- **⚠️ 位置は未 triage**（末尾は AI の既定）。

---

### AF. loop エンジニアリングは AIDD と別の層にあるが、実プロジェクトでは要る（2026-07-30 起票・優先度低）

- **問題:** 本ガイドは**1回のセッションの中で人間が裁定する**進め方を主題にしている。⇒ **AI を反復的に走らせ続ける運用（loop）を扱っていない。**
- **論点の出所:** [addyosmani.com「loop engineering」](https://addyosmani.com/blog/loop-engineering/)（⬜ **未読・本項は起票のみ**）。
- **著者の見立て `[Assumption]`（未検証）:** ①AIDD とは**層・軸が違う**が実プロジェクトでは必要になる ②**本ガイド自身が本体から Skill を実行している**ため、その一部は既に実演されている ③**新セクションの追加**になりそう。
- **🔴 検証の制約 `[Fact]`（著者申告）:** loop の検証は**トークン消費が大きく、現行プランでは枠が足りない見込み**。⇒ **ミニマムな対象で回して検証するサイクルを設計しないと着手できない。** 📌 **本ガイドのコスト構造の記述（[`01`](01_ai-driven-dev-strategy.md) §3.2）に照らすと、loop は避けるべき側を自動で増やす運用である**——§3.2 は主ドライバとして **出力（生成）量** を挙げ、また **長い履歴のキャッシュ読み出しの累積** を理由にトピック別のセッション分割を勧めている。⇒ **AI を反復的に走らせ続ける運用は、その両方を増やす方向に働く。** ⚠️ **ただし本ガイドは loop 運用のコストを測っていない** `[Assumption]`——**これは向きの推論であって実測ではない**（往復のコストについての測定は項目 `W` を参照）。
- **⚠️ 優先度は低い**（著者判断）。**本項は「扱っていない」ことを明示的に記録するために起票した**——**書かれていない領域は、読んでも分からない**（項目 AC と同じ理由）。
- **⚠️ 位置は未 triage**（末尾は AI の既定）。

---

### AG. 実戦の気づきを投入する経路が無く、その実装は「public 側の実行主体が private の構造を知る」問題を含む（2026-07-30 起票）

- **問題:** 本ガイドは**知見を書き戻す先**（[`docs/design-decisions.md`](docs/design-decisions.md) / 本ファイル / 章本文）を定めているが、**投入の入口が定型化されていない**。⇒ **実戦で得た気づきは、毎回その場の散文として渡され、分類は AI の判断に委ねられる。**
- **案（未着手）:** 気づきの投入を **Skill として実装する**。⚠️ **これは項目 H（方法論を Skill として活性化する）の具体例のひとつだが、H の他の候補と違う性質を持つ**——**公開リポジトリで実行され、書き込み先は非公開側**になる。
- **🔴 設計上の論点（未決）:** **public 側の Skill が、非公開側の構造（置き場所・ファイル分割）を知ることになる。**
  - 📌 **これ自体は新しい露出ではない** `[Fact]`——[`CLAUDE.md`](CLAUDE.md) は既に著者環境での実体パスを1行持っており、**「このパスを設定値として持っているのはここだけ」**と明示している。⇒ **Skill が同じ値を再び持つと、この設計が壊れる**（[`docs/design-decisions.md`](docs/design-decisions.md)「同じ情報を2箇所に置かない」）。
  - ⇒ **論点: Skill は置き場所を持たず、`CLAUDE.md` の1行を参照する形にできるか。**
- **⚠️ 分類の判定を自動化してはならない `[Judgment]`:** 投入されたものが**公開できる内容の判断か、書き手の生活・稼働の記録か**の振り分けは、本ガイドが繰り返し誤ってきた箇所である（本ファイルの起票が非公開側へ移された前例がある）。⇒ **Skill は「分類の候補を出す」までにし、裁定は人間に残す。**
- **📌 項目 AB の実装案と同じ判断を含む。** どちらも「非公開側の構造を、公開側の何かが知る」形になる ⇒ **2件まとめて設計する。**
- **⚠️ 位置は未 triage**（末尾は AI の既定）。

---

### AH. 「時限付き」として隔離した表に、時限を持たない記述が混ざっている（2026-07-30 起票・自己検出）

- **前提:** [`01`](01_ai-driven-dev-strategy.md) §2 と [`README`](README.md) は、モデル表を**「原則」と「2026-07 時点の当てはめ（時限付き）」の2層**に分けている（`v1.4.5`）。⇒ **この隔離自体は正しいが、中身の振り分けが不完全である。**
- **実測 `[Fact]`（2026-07-30）:** 「当てはめ」列に**製品名でない一般記述**が混ざっている——`01`＝「別系統の高性能モデル」「大容量コンテキスト特化モデル」／`README`＝「まず新セッション＋`@`参照（同一モデルで可）／できれば別系統の高性能モデル」。⇒ **これらは世代交代で古くならない**ので、原則層の内容である。
- **🔴 なぜ問題か:** **腐るものと腐らないものが同じブロックに入っている。** 隔離の主張は「**この表が古くなっても、上の原則は古くならない**」だが、**表の中に古くならない行があると、読者は表全体を時限付きとして割り引く**。⇒ **隔離が、守るはずだった記述まで一緒に減価させている。**
- **実測 `[Fact]`（2026-07-30）— 同じ表が2ファイルで分岐している:** `01` は5行、`README` は4行で、**`README` に「定型的なコーディング」行が無い。** ⇒ **既知の重複（DRY 違反）は、単なる二重管理ではなく既に内容がずれていた。**
- **⚠️ 語のドリフト `[Fact]`:** `01` の原則は「その時点で**最上位の推論モデル**」だが [`README`](README.md) の対応箇所は「**高性能モデル**」。⇒ **表の直下で「性能の比較表ではありません」と書いた語と衝突しかけている。**
- **論点（未決）:** ①一般記述を原則層へ移すか、当てはめ列を製品名だけに絞るか ②`README` の表を削除して `01` へ導線を張るか——⚠️ **`README` は入口なので影響が読者に直接出る**。**訂正と構造変更を1つの差分に混ぜない**（項目 Z）ため、**この2つは分けて実施する** ③「高性能」という語を repo 全体で見直すか（「射程」の sweep と同型）
- **⚠️ ガイド本体なので版チェックが発火する。** ⚠️ **位置は未 triage**（末尾は AI の既定）。

---

### AJ. README が、現在のリポジトリのコンテキストを反映していない（2026-07-30 起票・外部 AI のレビューが起点）

- **問題:** [`README`](README.md) は公開初期に書かれた構成のままで、**その後に変わったこと**（運用モードへの移行・確度ラベルの軸の置き換え・版の重ね・訂正記録の蓄積）が**入口の説明に反映されていない** `[Assumption]`。⇒ **読者が最初に受け取る像が、現在の中身とずれている可能性がある。**
- **起点 `[Fact]`（2026-07-30）:** **本リポジトリを別系統の AI に読ませたところ、この指摘が出た。** ⚠️ **読ませたのは公開リポジトリのみ**（非公開側は渡していない）。⇒ **項目 H の「段階3（別系統モデルによる再検証）」が、本ガイドの外で1回実行された記録である。**
  - 📌 **これは項目 H の活性化状況に効く** `[Judgment]`——**系統的なクロスレビューは依然未実施**だが（[`README`](README.md)「生成AIの利用について」の記述は維持される）、**単発の実行例が1件増えた。** ⚠️ **1件を「実施した」と読み替えない**（`v1.4.5` で同じ型の overclaim を起こしている）。
- **⚠️ 本項が扱わないもの:** **なぜ今 README を見直したいかという動機**は、公開しない判断（著者裁定・2026-07-30）。⇒ **本項の範囲は「入口の記述が現在の中身と一致しているか」に限る。**
- **論点（未決）:** ①どの節が古いかの棚卸し（現状は「ずれている感じがする」までで、**具体箇所が特定されていない**）②項目 AH（モデル表）と同じ場所を触るので、**まとめるか分けるか** ③「短時間で中身を確かめたい方へ」の3点が、いま最も見せるべき3点のままか
- **⚠️ ガイド本体なので版チェックが発火する。** ⚠️ **位置は未 triage**（末尾は AI の既定）。

---

## 知見の活性化（`.md` の方法論 → 実ツールで発火する Skill へ）

`.md` に書いた方法論は、Skill 化・`@` 参照して初めて「適用」される。**「本文に書いた」は取り込みであって適用ではない。**

| 方法論 | 出典（正本） | 活性化の形 | 配備先 | 状態 |
|--------|-------------|-----------|--------|------|
| クロスレビュー | `01` §2 / `05` §1.5 | `cross-review-playbook` Skill | repo `.claude/skills/` | ⬜ **未着手**（実物なし・**項目 H**。2026-07-24 の PBR で発展課題へ起票。⬜ の中で自己発火・検証の見込みが立つ筆頭候補） |
| 要件・仕様レビュー | `06` 全体 | `requirements-review` Skill | repo `.claude/skills/` | 🟡 **実物あり・1回発火**（項目 A。初版 2026-07-22。同日 `zenn-article` の WHAT 壁打ちで自己適用し AC1〜4 が実発火）。**格上げ条件＝2回目の発火**（1回で断定しない） |
| コードレビュー | `05` §1.3 | `code-review` Skill | repo `.claude/skills/` | ⏸ **保留**（項目 A。自己発火する対象コードが無い＝将来のコードプロジェクト待ち） |
| 技術記事執筆・overclaim レビュー | §6 / 項目 L / §4 / `01` §2 | `zenn-article` Skill | repo `.claude/skills/` | ✅ **活性化済み**（初版 2026-07-22。壁打ち1回＋レビュー1回で AC 実発火し、**公開成果物1本**に到達＝[Zenn 記事](https://zenn.dev/mryo0826/articles/a51445252edaab)・2026-07-23公開）。優先順位の判断は [`docs/design-decisions.md`](docs/design-decisions.md) §6 |
| 確度ラベル | `01` §2 | ①`CLAUDE.md` のチーム共通ルール ②各 Skill から共通参照 | ①配備済 ②未定 | 🟡 **部分的**（①は発火中——誤りを複数回検出した実績あり。②は参照先の Skill が未着手） |
| 最小ロード再開 | `CLAUDE.md` / `01` §3.2 | `session-resume` Skill | repo `.claude/skills/` | 🟡 **部分的**（`CLAUDE.md` の再開手順として発火中）。**Skill 化は却下（2026-07-24）**——同じ手順が2箇所になり「同じ情報を2箇所に置かない」に抵触。据え置きが正しい状態 |
| セッション終了の申告 | `01` §3.6 / `CLAUDE.md` | `CLAUDE.md` のトリガー1行 | 配備済 | 🟡 **配備済・効果未検証**。⚠️ **評価条件「数セッション運用」の運用セッション数を計数していない** `[Fact]`（表にも作業日誌にも記録なし）＝評価が測定不能のまま。項目 Y 系（状態記述の未追随）に近い |

> **次の記事化候補（`zenn-article` の2本目以降）:** ①**設計思想の記事**（重い方・#1 の設計意図外化が済んでから／イントロ記事の反応も見て）②**temp.md で思考を階層化する AIDD Tips**（軽い方・項目 N が源）③**effort/モデル選択の検証記事**（版固定＋blind replay で選択基準を実測したプロセス。強み＝対象を commit で示し再現できる実験ファイルをセットで出せる／blind 条件の設計と破綻もセットで見せられる／「引退するモデルをあえて回さなかった」抑制自体がコンテンツ。**正本は作業日誌側の実験ファイル**）。⚠️ **記事と本文は同じ源なので二重管理にしない**（片方を正本、片方を参照）。

> **状態ラベル:** ✅ **活性化済み**（実物あり・複数回発火・**公開成果物に到達**）／🟡 **部分的**（実物はあるが、発火回数か検証が足りない）／⬜ **未着手**（実物なし）／⏸ **保留**（着手しない理由が明示されている）。**✅ の条件に「公開成果物」を含めるのは、発火するだけでは一周しないため**（`docs/design-decisions.md` §6）。

> **この表の読み方:** ⬜ と 🟡 が並んでいることこそが本ファイルの要点である。**方法論を書いた量と、それが実際に発火している量は一致しない**——これは本ガイドに限らず、ドキュメントを書く行為に共通する落とし穴である。**2026-07-23 に初めて ✅ が付いた**——それまでこの表に ✅ は1つも無かった。

> **配備先の方針:** ポートフォリオとして示す Skill（`requirements-review` / `code-review`）は repo `.claude/skills/` に置き、clone だけで実物が伝わるようにする。純粋に個人用途の Skill は `~/.claude/skills/` へ配備する（`setup-claude-code.md` の手順）。

---

## 未解決論点

- **セッション終了・レトロの発火条件（設計済み・効果は未検証）:** 分担設計で定義した（`01` §3.6）。**残り:** 実際に運用して申告が有効に働くか（申告頻度が煩わしくないか、人間が裁定を返すか）は未検証。数セッション回して評価する
- **Spec Kit フルフローの本格運用:** 対象タスクでは軽量運用（仕様 Markdown ＋ Skills）で足りており、**フルフローは未実証**。中〜大規模・多論点のタスクが来たら実際に試し、Token 消費や使い勝手を検証する
- **既存コードベースでの AI 駆動 TDD の拡張:** 基本パターンは実証済み。**より複雑なリファクタリング（依存関係が深いもの）での適用は未検証**
- **ファイルベースの TODO 管理（`tasks/todo.md` 相当）:** 補助パターン扱い。エージェント組み込みの TODO 機能やセッション内の指示で足りるケースが多く、保守コストが見合わない可能性。必要性が実証されたら再検討する
- **他ツールへの展開 / ツール中立化:** 本ガイドは Claude Code native へ統一済み。別のエージェントツールへ展開する場合は、再利用可能な方法論（Skill／メモリの概念）とツール別のマッピングを分離して検討する

---

## 発展課題に関する補足（タグからは読めない情報）

`<!-- BRAINSTORM -->` タグの一覧は `grep` で得られる。ここには**タグを見ただけでは分からない実証状況**だけを残す。

- **MCP と CLI の判断フロー（`02` §1.3）:** 一部実証済み。**残り:** 特定の MCP 以外（データベース系 MCP 等）での判断適用が未検証
- **serena MCP の設定手順・運用フロー（`02` §2.2 / §5）:** 一部実証済み。**残り:** チーム展開時の設定共有フロー・初回セットアップ手順が未整備
- **「プロジェクト種別ごと」の論点（`01`・`03`・`05` に分散）:** 実際に取り組む際は1箇所（`05` が適切）にまとめ、他から参照する形を推奨
